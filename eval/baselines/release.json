{
  "version": 1,
  "cases": [
    "write-basic",
    "sum-numbers",
    "read-extract",
    "bash-count",
    "json-field",
    "filter-lines"
  ],
  "arms": ["baseline"],
  "minPassRate": 1,
  "caseMinPassRate": {
    "write-basic": 1,
    "sum-numbers": 1,
    "read-extract": 1,
    "bash-count": 1,
    "json-field": 1,
    "filter-lines": 1
  },
  "maxTotalTokens": 150000,
  "maxTotalWallMs": 300000,
  "notes": "EVAL-03c release quality/cost/latency gate. Same floors as nightly after #33646201722 evidence. Release re-runs this subset on the tagged commit (not reuse prior nightly artifact) so the candidate itself is measured. Must not be looser than nightly.json."
}
