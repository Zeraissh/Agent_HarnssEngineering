# Devin / 异步委托者 · 走查卡

- **角色：** Devin / 异步委托者（看板是家；交出去、过一会收 PR / 产物，不盯每一步工具调用）
- **表面：** 仅 Web http://127.0.0.1:4173/#walk=devin
- **日期：** 2026-09-14
- **活 UI：** 是。本机 `127.0.0.1:4173` HTTP 200，页标题「FATHOM 控制台」。开场 `/ready` 写 `activeRuns: 0`。Cursor 内置浏览标签对本会话挂不上，改用本机独立 Chrome（CDP 9247，`#walk=devin`），不共用别人的标签。
- **习惯伸手：** 找看板 → 派一张票 → 走开 → 回来收 PR / 产物 / 通知。不盯每一次工具调用。刷新后还在跑才算真异步。
- **本卡范围：** 统一剧本第 1、4、5、6 步（冷启动 / 稍长任务 / 中途停止·刷新 / 换地方）。

---

## 0. 开场（本卡自己的标签）

打开 `http://127.0.0.1:4173/#walk=devin`。标签页标题是「FATHOM 控制台」。截屏：`eval/persona-ux/walks/_devin-1-cold.png`。下面只记屏幕上的字。

---

## 1. 冷启动

第一眼不是看板，也不是「待领取 / 进行中 / 待评审 / 已出 PR」列。左边是对话列表，中间是欢迎页，右下角盖着新手卡。

中区大字（原文）：

> AGENT CONSOLE
> FATHOM.
> see every run to the bottom.
> 每一层都看得见。

侧栏能读到的字：`FATHOM.`、`Work`、`Code`（Code 亮着）、`新建对话`、搜索框「搜索对话…」。列表已经是别人留下的整句任务，按目录分组（`Agent_Design`、`ui-qa`、`T5-live-mu-chat`…），不是票号、不是 PR 链接。

顶栏有铃铛「通知中心」，当时没有数字角标。底栏五个图标读屏名：`打开指挥中心` / `打开产物` / `打开定时任务` / `记忆` / `打开设置`。

输入栏：项目「未入项（按目录）」；工作目录当时停在别人的 `ui-qa`；模型「环境变量 · deepseek-v4-flash」；标签「任务描述」；占位「要「ui-qa」做什么…」；勾选项「独立核查」；读屏名「运行任务」的上箭头。欢迎卡三张：「从计划开始 / 先对齐做法，再动代码」「看看这个仓库 / 用一段话说明项目在做什么」「修一处并跑通测试 / 改代码，用测试当判据」。

右下新手卡第 1/4 页原文：

> 初次使用
> 先圈定工作目录
> 工具写入圈是白名单里的全部目录。主目录是这次的焦点；其它已添加的项目也可以直接改，不必绕路。
> 1 / 4
> 跳过
> 下一步

| 判定 | 证据 | 我本来以为会…… |
|---|---|---|
| **不习惯** | 打开是「AGENT CONSOLE / 每一层都看得见。」加一长串聊天标题。没有「To Do / In Progress / PR Review」列，也没有「New ticket」。 | Devin 打开就是看板：派一张票、走开。 |
| **不习惯** | 发送按钮读屏名是「运行任务」。输入占位是「要「ui-qa」做什么…」。 | 交的是 ticket / session，不是坐在工位上「运行」一次。 |
| **不实用** | 新手第一页在教「先圈定工作目录 / 工具写入圈是白名单」。欢迎卡是「改代码、跑测试」。 | 异步委托者不圈工作目录。第一页该问「仓库 / 分支 / 回来收什么」。 |
| **好** | 顶上有「通知中心」，底下有「打开指挥中心 / 打开产物 / 打开定时任务」。 | 至少有通知、总览、产物、后台定时这几个入口，值得伸手点，不像纯聊天室。 |

伸手 30 秒：眼睛找看板和「Assign」，首页没有。手去点底栏和铃铛，不往输入框打字。

### 1b. 伸手去找板 / 通知 / 产物 / 后台

点底栏「打开指挥中心」，地址变成 `#/board`。顶栏三个数当时是：`1 运行中` / `0 待决定` / `46 今日完成`。三块标题：`待你决定`（「没有等你决定的事」）、`运行中`（别人的 `hello-copilot.txt`，有「停止」）、`最近完成`（完成 / 被停止）。截屏：`_devin-1-board.png`。没有「To Do / In Progress / Review / PR」列，也不能在这块板上「新建一张票」。

点顶栏「通知中心」。弹出层原文：`通知中心` / `全部已读` / `运行完成 1` / `完成` / 别人的 hello-copilot / `刚刚`。没有未读角标数字。截屏：`_devin-1-notify.png`。没有邮件、Slack、PR 链接。

点「打开产物」，地址 `#/artifacts`。顶上「产物」+「新建」。能看见一张「幻灯画册 · ui-qa · 2026-09-12」。不是 diff，不是 PR。截屏：`_devin-1-artifacts.png`。

点「打开定时任务」，地址 `#/schedules`。顶上「定时任务」+「新任务」。已经有别人留下的 `T5-draft-disabled`（「T5 草稿，禁止触发…」「一次性 · 2027年9月14日」「已停用 / 上次运行：未运行过 / 启用 / 立即运行 / 删除」）。本卡没点启用，也没点立即运行。截屏：`_devin-1-cron.png`。

全程没看见「回放 / replay」四个字。

| 判定 | 证据 | 我本来以为会…… |
|---|---|---|
| **好** | `#/board` 指挥中心把运行中 / 待决定 / 最近完成摊在一屏，铃铛会冒「运行完成」。 | 走开之后还能回来看总览，不必翻聊天。 |
| **不习惯** | 板是「运行状态收件箱」，不是可派票的看板。新建入口仍是侧栏「新建对话」+「运行任务」。 | 在板上按「New ticket」写仓库和验收，状态从 Queued 走到 PR。 |
| **不习惯** | 产物页是「幻灯画册」，定时任务是日历式 cron。通知只有「运行完成」一条本地气泡。 | 回来收的是 PR / 可下载 diff；通知是邮件或 IM；后台任务是交给云端还在跑的 session，不是 2027 年的一次性闹钟。 |
| **不实用** | 板上的「停止」对着别人正在跑的票。定时任务上别人写了「禁止触发」。 | 我的板只显示我派出去的票，不会跟工位上所有聊天混在一起。 |

---

## 4. 稍长一点的事（派票后走开）

回到欢迎页，点「新建对话」。点开「运行设置」，屏幕原文：

> 领域包 / 不用领域包
> 自动匹配 / 按任务选已注册的包
> 权限 / 自动 · ask 级会自动放行；deny / 圈禁 / 硬拒仍拦住
> 计划 / 拆解后等人批准
> 多 agent / 并行子任务，最多 3
> 自动放行 / 工具调用不再逐次问（已勾上）
> 思考强度 / 低
> 评分表 / 可选

输入栏勾选项仍是「独立核查」（未勾）。占位「要「ui-qa」做什么…」。工作目录下拉里能看见 `Agent_Design` / `ui-qa` / 几个 scratch，没有「选一个 GitHub 仓库、回来收 PR」。截屏：`_devin-4-settings.png`。

我认「自动放行」（走开时别每步问我），不认「领域包 / 独立核查 / 评分表」。「计划 · 拆解后等人批准」像要我坐着改计划，本票不勾。没改权限档，当时已经是自动放行。

输入原文：

> 列一下这个目录有什么，再写一份只有三行的说明到 devin-dir-note.txt。

点「运行任务」。地址变成 `#/run/5d6a3212-1716-4be7-99db-0dbfb20228e5/loop`，顶上「FATHOM · RUN 5D6A32」，中间「等待模型响应… / Thinking...」，发送钮变成「停止」。截屏：`_devin-4-after-send.png`。右侧已经摊开 Progress 和「变更 / 本次运行没有写盘操作」，中间出现工具原文 `update_progress items=[...]`。我没有点开这些，去指挥中心。

指挥中心当时：`4 运行中` / `0 待决定` / `47 今日完成`。我的票在「运行中」里（同一句出现两行）：`第 5 轮 · 已耗时 不到 1 分钟`，下面还跟了一句 `最近：bash · wc -l devin-dir-note.txt`。旁边是别人的 hello-lingma / approve-copilot。截屏：`_devin-4-board.png`。`/ready` 当时 `activeRuns: 2`。

| 判定 | 证据 | 我本来以为会…… |
|---|---|---|
| **好** | 「自动放行 / 工具调用不再逐次问」能勾上；板上「待决定」是 0，没有一张审批卡把我叫回来。 | 派出去就自己干，不要每一步工具都问。 |
| **好** | 走开后指挥中心「运行中」能看见我这张票，还有「停止」。 | 回来只看板上状态，不盯聊天。 |
| **不习惯** | 交出去的入口仍是「运行任务」，交完立刻进 `RUN 5D6A32` 直播：Thinking、Progress、工具 JSON。板卡上还写最近一条 bash。 | 派票后默认回看板，过程折叠。不要把每一条命令推到脸上。 |
| **不习惯** | 设置里第一排是领域包 / 核查 / 多 agent / 评分表。计划开关的说明是「拆解后等人批准」。 | 计划是一拍就可以走开的 spec，不是坐着等人改条目。包和核查不是派票语言。 |
| **不实用** | 同一张票在运行中出现两行；侧栏一组在「（默认工作目录）」，一组在 `ui-qa`。板上混着别人的票。 | 一张票一个卡，只显示我派的。回来收的是 PR，不是再读一轮 bash。 |

走开大约半分钟再回指挥中心：我的票已经在「最近完成」——`完成` / `1 分钟前 · 耗时 不到 1 分钟`。没有 PR 号。截屏：`_devin-4-back.png`。点侧栏这张票回到 `#/run/5d6a3212-…/loop`。右侧有 `产物 / 预览 / 下载 / 在文件夹中显示`，`变更` 写 `devin-dir-note.txt · 写入 · 505 B`。屏幕上没有「回放」「续跑」「Pull Request」。底栏输入占位变成「接着说…」。截屏：`_devin-4-ticket2.png`。磁盘上这份说明在 `ui-qa\devin-dir-note.txt`，三行。

回来收的是一份本地 txt + 聊天稿，不是 PR。

---

## 5. 过程中途（停止、刷新、还在不在）

再派一张稍长的票：`先列出这个目录的文件名，再写一份至少二十行的说明到 devin-stop2-note.txt。写完再用 bash 核对行数。` 地址 `#/run/3c275907-efc7-4ce8-8d64-a593b93b8b27/loop`（`RUN 3C2759`）。交出去约一秒就点「停止」，没有盯工具。

屏幕原文：

> 已停止 · 这次运行由你主动停止；已完成的工具调用与写入不会回滚。

底栏从「运行任务」变成「追加指令」+「继续对话」，占位「接着说…」。没有「回放」。截屏：`_devin-5-midstop.png`。

刷新同一标签。地址还是 `#/run/3c275907-…/loop`，停的那句话还在，侧栏这张票还在。`回放=false`，`继续对话=true`。`/ready` 的 `activeRuns` 变成 0。截屏：`_devin-5-refresh.png`。停掉的任务没有在后台偷偷接着跑。

再派一张不点停止的票：`…devin-bg-note.txt`（`#/run/542760fe-…`）。交出去立刻刷新。刷新后还在同一条 run 地址上，`停止` 钮还在，`Thinking` 还在，`/ready` 写 `activeRuns: 1`。过几秒铃铛弹出：`通知中心 / 运行完成 1 / 完成 / …devin-bg-note.txt / 刚刚`。截屏：`_devin-5-bg-refresh.png`。刷新没有把没停的票掐死。

全程屏幕上没有出现「回放 / replay」。定时任务页仍然是别人的 2027 cron，不是这张活着的票。

| 判定 | 证据 | 我本来以为会…… |
|---|---|---|
| **好** | 指挥中心和运行页都有「停止」。停完写明「由你主动停止；已完成的…不会回滚」。 | 走开之后还能从板上把票掐掉，而且说实话有没有落盘。 |
| **好** | 刷新后同一 `#/run/…` 还在；没停的票 `activeRuns` 仍是 1，跑完铃铛报「运行完成」。 | 关页再开，云端还在跑，回来收结果。这是异步地板。 |
| **不习惯** | 停完只能「继续对话 / 接着说」。没有「回放」这条过程。 | 回来要能重放它干过什么，而不是从头再聊一句。 |
| **不习惯** | 停止后输入栏立刻又变成聊天。完成的票也是「接着说」。 | 一张关掉的票应该停在「已交付 / 被取消」，不要默认把我拽回对话。 |
| **不实用** | 第一张稍长票（`devin-stop-note.txt`）在我点停止之前已经「完成 · 不到 1 分钟」。短票几乎停不到。 | 异步票该够长，让我走开再回来还看得到「运行中」。 |

---

## 6. 换地方（项目 / 目录 / 记忆）

点输入栏「项目」，当时写着「未入项（按目录）」。下拉原文还能看见：`网盘管理`、`T5-live-mu-chat`、`＋ 新建项目…`，以及一串本机目录（`Agent_Design` / `ui-qa` / `AGS` / `live-mu-…/chat|py|plan|lingma` / `kimi-walk-20260914` / `＋ 添加目录…`）。截屏：`_devin-6-project.png`。没有「换一个 GitHub 仓库」。

点 `T5-live-mu-chat`。占位从「要「ui-qa」做什么…」变成「要「chat」做什么…」。侧栏只剩这个项目里别人的长句（八十个短句、U1-hold…）。我刚才派的 `devin-dir-note` / `devin-stop2` 从这张侧栏消失了，要回到「未入项」或指挥中心才能摸到。截屏：`_devin-6-switched.png`。工作目录按钮变成 `D:\Work\scratch\live-mu-20260914\chat · +1`。

点底栏「记忆」。弹出层原文：

> 记忆 D:\Work\scratch\live-mu-20260914\chat\.agent-memory
> 本项目 / 全部
> 还没有记忆——Agent 在跨会话工作中积累的内容会出现在这里

不是上一张票留下的说明文件，也不是 PR。截屏：`_devin-6-memory.png`。`＋ 添加目录…` 在菜单里，点「添加目录」没有出现本页表单（多半要系统选文件夹）。我的产物还在原来的 `ui-qa\devin-dir-note.txt`，换项目不会把它带过来。

| 判定 | 证据 | 我本来以为会…… |
|---|---|---|
| **好** | 项目能从「未入项」切到 `T5-live-mu-chat`，占位跟着改成「要「chat」做什么…」。指挥中心仍能列出跨目录的最近完成。 | 换地方之后还有一个总览能找到旧票。 |
| **不习惯** | 切项目侧栏就换成别人的聊天。我的票不跟着走。记忆是空的跨会话笔记，不是上一张票的产物。 | 换仓库 / 换项目时，我派出去的票和 PR 还在一块板上。 |
| **不习惯** | 换地方的入口是「工作目录」白名单 +「未入项（按目录）」，还要「添加目录」。 | 指一个 GitHub repo 或从看板换项目，不要先圈本机文件夹。 |
| **不实用** | 记忆空态和「网盘管理」都不给我上一张 `devin-dir-note.txt` 的下载或 PR。 | 换地方要能指着上次交付物说「下一张票接着这个」。 |

---

## 收口

- **走查卡路径：** `eval/persona-ux/walks/devin.md`
- **活 UI：** 是。本机 `http://127.0.0.1:4173/#walk=devin`（页标题「FATHOM 控制台」，`/health` 200）。Cursor 内置浏览标签挂不上，用独立 Chrome CDP 9247 点的，不共用别人的标签。
- **没走：** 剧本 2 / 3 / 7 / 8（一句话小事、批准卡、CLI、桌面）。本卡只要 1 / 4 / 5 / 6。
- **没点：** 别人的「停止」、定时任务上的「启用 / 立即运行」。

)
