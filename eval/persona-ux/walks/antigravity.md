# Antigravity / Kiro 惯用者 · 走查卡

- **角色：** Antigravity / Kiro 惯用者（并行 agent、产物截图/日志、先写 spec 再干；要的是 agent manager，不是一个聊天框）
- **习惯卡：** `eval/persona-ux/habits.md` 没有单独的 Antigravity / Kiro 卡。按评测计划那一行走：并行 agent、产物（截图/日志）、先写 spec 再干。
- **表面：** 仅 Web。自开标签 `http://127.0.0.1:4173/#walk=antigravity`（独立 Chrome CDP 9258，不共用别人的 Cursor 浏览器标签）
- **日期：** 2026-09-14
- **活 UI：** 是。本机 `127.0.0.1:4173` HTTP 200，`/ready` 写 `activeRuns: 2`。Cursor 内置浏览标签对本会话挂不上（`No browser tab available`）。自开独立 Chrome（CDP **9258**，user-data `eval/persona-ux/walks/_antigravity-chrome-profile`），地址 `http://127.0.0.1:4173/#walk=antigravity`，不共用 9229/9230/9231/9232/9235/9247/9343。
- **习惯伸手：** 找 agent 列表 / 并行任务，不是单聊输入框 → 找产物窗（截图、日志、浏览器回放）→ 动手前先找 spec / 计划卡 → 换任务时找 worktree，不是共用一个脏工作区。

盯的迁移缝：并行 runs / spec·plan 先于动手 / 产物窗 / worktree。

---

## 1. 冷启动

打开 `http://127.0.0.1:4173/#walk=antigravity`。标签原文：`FATHOM 控制台`。`html lang="zh-CN"`。截屏：`eval/persona-ux/walks/_antigravity-1-cold.png`。

第一屏是**两栏聊天台**，不是 agent manager。中区大字：

> AGENT CONSOLE
> FATHOM.
> see every run to the bottom.
> 每一层都看得见。

左栏主按钮是「新建对话」，搜索占位「搜索对话…」。底下已经堆着别人的对话（`ui-qa` / `lingma` / `Agent_Design` / `T5-live-mu-chat`），不是我的 agent 队列。输入栏标签「任务描述」，占位「要「ui-qa」做什么…」。旁边：项目「未入项（按目录）」/ 工作目录（当时落在别人的 `ui-qa`）/ 模型「环境变量 · deepseek-v4-flash」/「运行设置」/「独立核查」。欢迎卡三张：「从计划开始 / 先对齐做法，再动代码」「看看这个仓库」「修一处并跑通测试」。

右下角盖着新手卡，第 1 页原文：

> 初次使用 / 先圈定工作目录 / 工具写入圈是白名单里的全部目录。主目录是这次的焦点；其它已添加的项目也可以直接改，不必绕路。 / 1 / 4 / 跳过 / 下一步

第一屏全文检索：**没有**「agent manager / spec / 规格 / 需求.md / 设计.md / worktree / 工作树 / 截图 / 日志回放」。右侧预览和 `#artifact-canvas-view` 都是 `display:none`。底栏图标名称能点到：「打开指挥中心」/「打开产物」/「打开定时任务」/「记忆」/「打开设置」。

点「跳过」后打开「运行设置」，原文（截屏 `_antigravity-1-knobs.png`）：

- `领域包`（默认 `不用领域包`）
- `权限`：`自动 · ask 级会自动放行；deny / 圈禁 / 硬拒仍拦住`；选项 `自定义 / 逐次审批 / 计划确认门 / 自动放行 ask`（当时值 `auto`）
- `计划`：`拆解后等人批准`（**没勾**）
- `多 agent`：`并行子任务，最多 3`（**没勾**）
- `自动放行`：`工具调用不再逐次问`（**勾着**）
- 另有自动匹配、思考强度、评分表

底栏「打开指挥中心」进 `#/board`（截屏 `_antigravity-1-board.png`）。板上原文：`待你决定 / 没有等你决定的事`；`运行中 1`（当时是别人的 `hello-kimi-web.txt`，有「停止」）；`最近完成 30`。计数条：`1 运行中 / 0 待决定 / 55 今日完成`。这是一张**全宿主共用的运行看板**，不是我自己的并行 agent 列表，也没有 worktree 列。

底栏「打开产物」进 `#/artifacts`（截屏 `_antigravity-1-artifacts.png`）。标题「产物 / 新建」，格子里一张旧卡「幻灯画册 · ui-qa · 2026-09-12」。点进去变成 `#/run/…/artifact/0`，右侧画出 `deck-basic/index.html` 幻灯预览（放大 / 前往 / 导出 / 在文件夹中显示 / 点评）。**没有**「这次 agent 的截图 / 终端日志 / 浏览器回放」这类过程产物。全文仍 0 次 `worktree` / `工作树`。

| 判定 | 证据 | 我本来以为会…… |
|---|---|---|
| **好** | 底栏真有「指挥中心」和「产物」。指挥中心能同时看见运行中的别人任务；产物能打开一张幻灯并预览。欢迎卡第一张就写「从计划开始」。运行设置里有「计划」和「多 agent」。 | 家是 agent 工作台：能看见并行任务、能看见产物，动手前有 Plan。 |
| **不习惯** | 默认入口仍是「新建对话」+ 大输入框。指挥中心要再点一次才出来。产物是成品画册，不是当前 agent 的截图/日志窗。计划 / 多 agent 默认都关着。 | 打开就是 agent manager：并行 agent 卡、产物窗已经在右侧，先写 spec 再让人跑。 |
| **不实用** | 冷启动先考「写入圈 / 领域包 / 独立核查」。没有 spec 文件（requirements / design / tasks），没有 worktree。指挥中心里全是别人的对话标题。 | 我的 agent 各占一棵 worktree；产物窗跟这次任务走，不是全站聊天考古。 |

下一步：自己开对话，写一句小事，看从输入到看见结果要几步；同时盯批准卡和产物。

---

## 2. 一句话小事

点「新建对话」。把工作目录拧到 `D:\Work\Github_pros\Agent_Design`（条上变成 `D:\Work\Github…\Agent_Design`，并出现「当前分支 main *」）。权限选「逐次审批」，关掉自动放行和独立核查。输入：

> 在当前工作目录写一个 hello-antigravity.txt，内容只有一行：ping。不要读别的文件，不要跑命令。

点「运行任务」。立刻：「新任务已提交：…」地址变成 `#/run/c33b8c94-71e2-4fc4-9ed2-18168371ce69/loop`，条上有「停止」。过程是 `Thinking...` 然后工具原文（截屏 `_antigravity-2-after-submit.png`）：

```
⚠ write_file
{
  "path": "hello-antigravity.txt",
  "content": "ping\n"
}
允许本次 / 短期允许相同参数 / 拒绝并说明
```

点「允许本次」后：「运行已完成」+「已在工作目录创建 hello-antigravity.txt，内容为一行 "ping"。」磁盘文件就是一行 `ping`。同一屏产物条：`hello-antigravity.txt / 文档 / 产物 / 预览 / 下载 / 在文件夹中显示 / 变更`。旁边又写「本次运行没有写盘操作」。另有一块 `Progress ⟩` / `等待拆步…`，以及「验证 · 1 / 假设与前提 · 1」。没有截图、没有终端日志窗、没有 spec 文件。Run `C33B8C`。

| 判定 | 证据 | 我本来以为会…… |
|---|---|---|
| **好** | 写盘前停在工具 JSON；产物条能预览/下载这个 txt。磁盘真的是 `ping`。过程不是转圈十分钟。 | 产物能指到文件，而不是只有一段「我写好了」。 |
| **不习惯** | 从打字到看见结果要：拧权限 → 发送 → 等批准卡 → 允许本次 → 再读一段「已创建」。没有先出 spec。`Progress / 等待拆步` 出现在**写完之后**。 | 先写 spec / 计划卡，人改完再动手；产物窗跟这次 run 的截图和日志。 |
| **不实用** | 「已创建」和「本次运行没有写盘操作」对着干。一行 ping 还挂「验证 / 假设与前提」。 | 真相只有一份；小事不要拆步仪式。 |

---

## 3. 要批准的事

上一步的写文件卡就是批准面。条上权限原文：「手动 · 危险动作会先问你，不会自动放行」。按钮：「允许本次 / 短期允许相同参数 / 拒绝并说明」，另有「自动放行 ask」。

| 判定 | 证据 | 我本来以为会…… |
|---|---|---|
| **好** | 卡片上是将要写的路径和内容，不是只有工具名。拒绝在。写盘前能停。 | 动手前能看见影响面。 |
| **不习惯** | 必须先把默认的「自动放行」拧到「逐次审批」，否则按设置页「ask 级会自动放行」。批准卡不是 spec 卡。 | 默认先出 spec；写文件是 spec 通过之后的事，不是第一张卡。 |
| **不实用** | 指挥中心里同时能看见别人的运行和「停止」。这是全站看板，不是我的 agent 沙箱。 | 每个 agent 隔离；我不会在批准自己的写文件时看到别人的任务。 |

---

## 4. 稍长一点 / spec 先于动手

再「新建对话」。勾「计划」（`拆解后等人批准`）和「多 agent」（`并行子任务，最多 3`）。权限选「计划确认门」。独立核查勾是关的，条上仍写「独立核查（计划编排默认仍核查子任务）」。任务：

> 列一下当前目录顶层文件名，再写一份只有三行的说明到 antigravity-note.txt。先写出 spec/计划等我改条目，不要直接写文件。

Run `56DB03`。先是 `等待模型响应…` / `Thinking...`，然后不是可改的 spec 文件，而是 `ask_user` 两道选择题（「三行写什么」「只数文件还是连子目录」），署名「◆ 施敢当」。点「都让它自己定」后出现 `submit_requirements`（文案带「计划，不落地任何文件」），以及 `✗ glob *`。

大约一分多钟后屏幕上是「编排计划 / 1 步 · 层宽 1 · 并行度 1」，子任务 s1 标题「起草可逐条修改的 spec（纯文字，不落盘）」，正文里有 S1–S17。按钮：「批准并开跑 / 否决（中止本次运行）」。指挥中心同时写着「计划待签发」**和**「运行中 / 最近：glob · */*」——签字前已经在扫目录。勾了「多 agent」，实际「并行度 1（auto）· 层宽 1 · 1 个子任务 · 拆解耗时 1m17s」。

点「新建对话」想并行再开一个 agent：欢迎态输入框回来了（占位「要「Agent_Design」做什么…」），但「◈ 计划待你签字」整张卡跟着到了新对话上，文案「计明远（planner）已拆出 1 个子任务…批准后才会发射第一个子任务」。S1–S17 **不能**在框里改——可编辑的只有任务输入、评分表、定时任务表单。截屏：`_antigravity-4-plan-mid.png` / `_antigravity-4-spec.png` / `_antigravity-4-gate.png`。

| 判定 | 证据 | 我本来以为会…… |
|---|---|---|
| **好** | 勾上「计划确认门」之后，真的出现「计划待签发 / 批准并开跑 / 否决」。计划正文用了「spec」这个词，还写成编号条目。指挥中心能看见这张待签卡。 | 动手前先有一份人能签字的计划。 |
| **不习惯** | 先考选择题，再出一份不能改的计划稿；glob 在签字前就跑了。多 agent 打开仍是 1 路。点「新建对话」计划卡还粘在身上。 | Kiro / Antigravity：requirements / design / tasks 落成可改文件；并行 agent 各开各的；manager 里再派一个不会把上一张卡拖过来。 |
| **不实用** | 「可逐条修改」写在计划里，屏幕上没有一条能划掉。关了独立核查，编排仍声明默认核查子任务。一层 s1 为了一份三行便签拆了 1 分 17 秒。 | spec 是我能改的文件；小事不要编排仪式。 |

---

## 5. 过程中途（停止 / 历史 / 刷新 / 产物）

指挥中心当时：`待你决定 1`（就是 56DB03 的「计划待签发」）+ `运行中 1`（同一条，还在 glob）+ `最近完成` 里能看见五分钟前的 `hello-antigravity.txt`。截屏 `_antigravity-5-board-pending.png`。

回到 `#/run/56db03f3-…/loop` 点「停止」。磁盘没有 `antigravity-note.txt`。停完条上变成「运行已完成：列一下当前目录…」，同时「批准并开跑 / 否决」还在，「继续对话」出现。刷新同一条 run：停止键没了，计划签字钮还在，历史对话还在。侧栏点「在当前工作目录写一个 hello-antigr…」回到 `C33B8C`，产物条和 `ping` 还在。

底栏「产物」仍是全站画册（旧「幻灯画册」），不是这次 run 的截图/日志。这次 run 的「产物」就是对话里那张 txt 卡片。

| 判定 | 证据 | 我本来以为会…… |
|---|---|---|
| **好** | 运行中有「停止」，点下去后没写出 `antigravity-note.txt`。刷新和侧栏能找回 hello 那次的结果与产物。指挥中心能同时看见待签 + 运行中。 | 中途能打断；上次产物还在。 |
| **不习惯** | 「已停止」没单独成句，顶栏说「运行已完成」，计划签字钮刷新后还在。没有从停止处接着写 spec。 | 停就是停；回来能续同一条正史，或从产物窗打开截图/日志。 |
| **不实用** | 产物窗是别人的幻灯，不是 agent 过程证据。 | 每个 agent 自带截图、浏览器回放、终端日志。 |

---

## 6. 换地方

工作目录从 `Agent_Design` 换成 `D:\Work\scratch\live-mu-20260914\chat`。再点「新建对话」，占位变成「要「chat」做什么…」，条上是 `D:\Work\scratc…20260914\chat`。侧栏分组还在：`Agent_Design` 底下仍能点回 hello-antigravity；还有 `ui-qa` / `kimi-walk-20260914` / `lingma` / `T5-live-mu-chat`。设置页标签是「外观 / 模型 / MCP / Skills / 领域包 / 运行默认值 / 通知 / 快捷键 / 消耗 / 关于」。全文 0 次 `worktree` / `工作树`。换目录只是下拉换路径，没有「给这个 agent 开一棵隔离 worktree」。

| 判定 | 证据 | 我本来以为会…… |
|---|---|---|
| **好** | 换目录后占位跟着文件夹名走；旧目录的对话还在分组里。 | 换地方不会把上次结果弄丢。 |
| **不习惯** | 换的是共用工作目录列表，不是 per-agent worktree。设置里也没有工作树入口。 | 每个并行 agent 一棵干净 worktree，做完再收。 |
| **不实用** | 「＋ 添加目录…」是白名单圈地，不是派活隔离。 | 新任务默认隔离；不会跟别人的 hello-*.txt 写在同一个脏根上。 |

---

## 伸手对照（并行 / spec / 产物 / worktree）

| 伸手要什么 | 屏幕上实际有什么 |
|---|---|
| Agent manager，不是单聊 | 默认是「新建对话」+ 大输入框。底栏「指挥中心」是全宿主运行看板（待决定 / 运行中 / 最近完成）。 |
| 并行 agent | 运行设置「多 agent · 并行子任务，最多 3」，默认关。勾上后这件列目录仍是「并行度 1」。指挥中心能看见别人同时在跑，但那是共享宿主，不是我派出去的两路 agent。新建对话会把未签计划卡粘过来。 |
| Spec / 计划先于动手 | 有「计划确认门」和「批准并开跑」。先 ask_user，再出不能改的 S1–S17 稿；签字前已经 glob。没有 requirements.md / design.md / tasks.md。 |
| 产物窗（截图 / 日志 / 回放） | 底栏「产物」是成品画册（幻灯）。run 内产物条是写出来的 txt。没有过程截图、没有终端日志窗。 |
| Worktree | 没有。只有工作目录下拉和侧栏按文件夹分组。设置里也不提。 |

**活 UI：是。** 路径：`eval/persona-ux/walks/antigravity.md`


