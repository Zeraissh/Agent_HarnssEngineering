# 真实任务案例 #7 — Web 宿主 UI × rubric-verifier 实战首秀（2026-08-05）

**任务**：harness 给自己盖房子——把 CLI 宿主升级为浏览器实时控制台(任务提交/事件流
直播/审批应答/三值裁决呈现)。历史最大交付物(6 文件 2436 行),自托管(agent 在
harness 自己的仓库里开发,src/** 禁改的纪律约束),执行者/核查者均 deepseek-v4-pro,
ts-coding 包(本案催生的第四个领域包)首战。

## 交付(两阶段均零返工)

| 阶段 | 交付物 | 轮数 | 核查 |
|---|---|---|---|
| s1 后端事件桥 | ui/server.ts(纯 Node 内置,零新依赖):SSE 重放+直播、审批回调挂起到 HTTP、verdict 合成事件 + 六场景契约测试 | 21 | 6/6 AC 第一手签字 |
| s2 前端+集成修正 | 零构建原生 ES modules:纯 reducer 与渲染分离、时间线/核查面板/审批卡/三值裁决卡 + 委托方审出的三处集成缺口修正 | — | 客观 6/6 + 四维 rubric 意见 |

**浏览器全栈实测**(flash 模型真跑):UI 提交任务 → 时间线直播 → 浏览器点击放行
write_file 审批(回调跨 HTTP 挂起/应答链路实证)→ verifier 现场演示诚实降级
(bash 被只读门拒 ×2 → 改 read_file 取证 → passed=true 且"字节数/换行细节缺工具
无法独立验证"如实落 unverified)→ 三值裁决卡带 🟡 待委托方复核渲染。
**harness 的 UI 直播了 harness 核查自己的过程**。

## rubric-verifier 生产首秀(AGENT_VERIFY_RUBRIC 首用)

s2 注入四维评分表(信息层级/语义一致性/可用性细节/代码可维护性),verifier 产出
四条 ◈ advisory,每条"维度 | 结论 | 依据"带具体证据(色值对照 CLI 语义、折叠阈值、
BEM 命名核对)——**主观意见与客观裁决(162 tests/tsc)在同一份裁决里各行其道,
互不污染**:advisory 未触发返工,客观项决定 passed。三值协议在真实生产任务上
按设计工作。界面审美的最终裁决权留给委托方(用户浏览器亲验)。

## 结构性发现

1. **委托方审查与程序化门禁互补**:s1 全绿通过后,委托方代码审查仍抓出三处
   集成级缺口(缺省模型硬编码/compat 写死/verifier 审批双响风险)——门禁验证
   "做对了写的",审查发现"没写的"。缺口编入 s2 任务书闭环,零返工修复。
2. **回调跨进程挂起是事件桥的核心难点**:approval_request 的 respond 回调
   必须在服务端存活到浏览器应答;verifier 来源的审批已内部自答,再入挂起表
   即双响——事件流的"来源"字段承担了语义隔离职责。
3. **可测性结构约束是前端质量的地板**:任务书强制 reducer(纯函数)与 DOM
   渲染分离,使 UI 逻辑获得与后端同级的 vitest 覆盖——"零构建 + 可测"
   两个约束互相成就。
4. **ts-coding 包一次到位**:python-coding 的缺口清单(白名单/工具面成文)
   直接移植,TS 域首战零核查饥饿零返工——领域包的边际成本在快速下降。

## 状态

阶段 1+2 已提交(53c5f2c);UI 可用 `tsx <launcher>` 起在任意端口
(createUiServer({workdir, packName, modelClient}) 可注入)。待办:委托方
浏览器终审(界面审美的 rubric 意见仅供参考,裁决权在人),视反馈决定 s3 打磨项。
