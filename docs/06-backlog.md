# 待办清单（v2 UI 之后）

# 交接：从这里接着做（2026-09-02 成熟度第一波）

**进行中**：按 `docs/08-maturity-optimization-checklist.md` Phase 1 第一波推进
（覆盖率门 → 台账/统计 → mock 确定性评测 → provider 降级 → nightly）。
单操作员形态；GOV-* 与 SAFE-05 Phase 2B **本波不做**。
每项独立 commit + push，等 CI 绿再开下一项。

**已落地**：
- TEST-01a — `npm run test:coverage` + `npm run test:mutation-smoke`（8/8 killed）；CI artifact。
- EVAL-02 — 台账 `error` 分类 + `npm run eval:stats`（Wilson/pass@k/taxonomy）。
- EVAL-03a — `npm run eval:deterministic`（12 场景跑 `dist/` + mock；CI `deterministic-eval`）。

以下为历史交接页（2026-08-08 收工），仍有参考价值；新开工优先看 `docs/08`。

---

# 交接：从这里接着做（2026-08-08 收工）

**至 `ddcc18a`（2026-08-09 晨）**：**784 单测 / 22 文件 + `tsc` 全绿**｜工作树干净。
B0（planner 三项修复）、B1（终止原因口径收敛，顺带修出 plan_gate_expired 潜伏缺陷）、
B2（运行历史落盘）、B2b（backlog 补标注）、台账 `[object Object]` 修复——五个提交全部收口。
**B 节已清空**（只剩 B3），下一步的主线在 C1（案例 #9）与 A1（攒 §2.1 样本），两者可一次做。
上一轮 24 个提交，主题是**委托方界面反馈四步**（对话主干化 → 产物清单 →
装配状态条 → 低切换成本三件套），外加流式匀速放行、代码高亮、贴底跟随、
`.env` 装载、L6 运行台账。

## 开工前必看（三条，读完再动手）

1. **判据先写、数据后收。** 上一轮做 L6 台账时把 §2.1 的阈值写死在
   `src/ledger.ts` 的 `STRUCTURED_OUTPUT_RULE` 里，**在拿到任何数据之前**。
   别为了让结论好看去改它——真要改，先说清为什么原来的判据错了。
2. **复验一条验收要用变异测试**（发现 46）：把它声称保护的东西改坏，
   看有没有测试变红。上一轮靠这招在"全绿"的仓库里挖出 4 处真缺陷。
3. **真机结论先问"这是被测对象还是我这套观测装置的性质"**（发现 49）。
   上一轮两次把自动化面板的行为（不合成画面、节流定时器）当成产品缺陷，
   其中一次还写进注释当实证。**优先选不受装置影响的判据。**

## 环境（已就绪，别重复配）

- `.env` 已配好（deepseek 兼容端点 + key），`npm run ui/cli/eval` 会自动读。
  **注意**：进程环境变量**压过** `.env`，启动时若看到冲突告警，
  先 `$env:ANTHROPIC_BASE_URL=$null` 再起。
- Web 宿主跑在 `127.0.0.1:4173`（委托方自己起的）。改了 `ui/server.ts`
  或 `src/**` 要重启才生效。**运行历史已落盘**（B2，`.agent-run-history/`）——
  重启不再丢历史；但**旧进程内存里的 run 不受益**（落盘是新代码写的，
  换到 ≥`ddcc18a` 的那一次重启仍会丢当时在内存里的运行），且归档 run
  **不能跨重启续跑**（界面会照实说）。
- 台账在 `.agent-runs.jsonl`（已 gitignore），`npm run ledger` 读数。

## 还开着的事（按优先级）

### A. 等证据类——不是"没做"，是"证据还不够"

**A1. §2.1 结构化输出：台账已就位，样本需继续攒。**
`npm run ledger` 读 `.agent-runs.jsonl`（gitignore）。判据要 ≥20 次**实施后**裁决才下结论。
→ **批量采集**：配置 API 凭据后 `npm run ledger:samples`（小任务 + `--verify`，自动补到 20）。
→ **单次**：CLI `npm run agent -- run --yes --verify "…"` 或 Web「开启独立核查」。
攒够了跑 `npm run ledger`，它会**直接给出效果结论**（`effective` / `insufficient` 等）。

**A2. 9.9 `write_memory` 观察项：改为自动检出，等第二次出现。**
台账按角色记工具直方图，verifier 侧的写类调用会单独列出。
→ **注意**：这个现象**只有 CLI + 领域包那条路能产生**——Web 宿主根本没接
`MemoryStore`（`ui/server.ts` 零命中）。要它出证据，得跑 CLI 带 pack 的真机任务。

**A3. NVDA 听感（AC-06 的非结构部分）。** 需要真人戴耳机听，自动化替不了。
结构层已由 axe 常驻门禁覆盖。

### B. 已确诊、可直接动手

**B0.（新，独立核对发现）planner 从来没拿到 verifier 拿到的那三项修复。**
**✅ 已实施（2026-08-08，769 单测全绿 + 七个变异全部被抓红）。** 落地与两条实施时才看清的事：

- `drain` → `drainPlannerEvents`（镜像 `drainVerifierEvents`，run 与 runContinuation 共用）；
  9.7 收口续跑（`PLANNER_WRAPUP_MAX_TURNS = 2`）+ 9.2 `failureSummary` 过程摘要，
  freeform 与结构化协议两条路都接了；预算三级化 `AGENT_PLAN_MAX_TURNS` > 包
  `plan.maxTurns` > 默认 12（`resolvePlannerMaxTurns`），与执行者 maxTurns 解耦。
  宿主同提交接上：CLI 失败输出带摘要；Web 的 run_config/`/api/harness` 报
  `plannerBudgetTurns`+来源、`plan_result` 带 `plannerRecovery`/`plannerFailure`、
  plan 芯片与 Plan 面失败 callout 各有渲染锁。
- **多包预算的设计点**（B0 原文没写，动手时定的）：verifier 逐子任务按那个子任务
  的包取预算，planner 面对整个菜单（还没拆，不知道任务落在哪个域）——取声明值
  **最大**。预算是护栏不是配额，取大只允许不强制。"计划不该比执行贵"改由不等式
  测试锁（`plan.maxTurns ≤ guardrails.maxTurns`，且缺省 12 ≤ 全部包护栏），
  不再运行时夹断。**没给任何包填数**：verifier 的 30 是案例 #8 实测出来的，
  planner 侧还没有等价证据——机制先行，数字等实测。
- **那条"钉死修前语义"的测试，实际比交接页写的细**：它的脚本是 `end_turn` 空输出
  而非 `max_turns`，修后行为对该场景本来就该不变（模型自己收笔却没写东西，续跑
  与重问都救不了）。所以不是改判据，是把标题收窄成"end_turn 空输出"并**新增**
  `max_turns` 空输出 → 收口续跑的行为锁。核对交接页时值得记住：条目说"改那条
  测试"，到了现场先看它到底钉的是哪半边。

原始诊断（保留，作案发现场记录）：
9.1（预算按域可覆盖）、9.7（撞预算后收口续跑）、9.2（兜底裁决带过程摘要）
三件事都只做在 **verifier** 上。三角编排的第三个角色 **planner 一件都没有**：
- `src/planner.ts:140` 与 `:291` 两处**各写死** `Math.min(cfg.maxTurns ?? 50, 12)`
  ——包与 env 都覆盖不了，还会把 `presets.ts` 里声明的 25~40 轮护栏一律夹到 12；
- 全文件 `runContinuation` **零命中**：撞满预算就是半截工具调用 + 空文本
  → `runPlanner` 直接 fail-closed，整场拆解连同已取到的探索证据一起作废；
- `src/planner.ts:289` 的 `drain` 只返回 `{ text, usage }`，
  `stopReason` 与 `messages` **在这一层就被丢掉了**——不扩它，后面哪一项都做不了。

失效形态与 9.7 修前的 verifier **逐字相同**，而 planner 的预算（12）比
verifier 修前（15）还紧，撞上的概率更高；案例 #8 正是由 planner 拆的跨域任务。
更麻烦的是 `test/planner.test.ts` 里有一条测试把**修前语义钉死了**
（"空输出 → 不重问，无计划（fail-closed）"）——等于给这个缺口上了一道绿锁。

→ **第一步**：先把 `drain` 的返回值扩成 `{ text, usage, stopReason, messages }`
（这是前置，不扩后面都做不了）；再照 `src/verifier.ts:145` 那个分支的形状加
`stopReason === "max_turns"` 的收口续跑；预算那条单独走——把两处 `12` 提成
常量并接 `DomainPack`，与 9.1 的 env > 包 > 默认 三级同构。
改完记得改那条钉死旧语义的测试。

~~**B1. 终止原因的口径三处不一致：5 / 7 / 9。**~~ ✅ 已收口（2026-08-09，见条末）
`docs/03-interfaces.md:170` 列 **5** 值、`src/types.ts` 列 **7** 值、
而 `ui/public/app.js` 的 `classifyStopReason` 实判 **9** 个具名值
（多 `max_tokens` / `plan_rejected` / `plan_gate_expired`）。三处都在被引用。
**另有一处直接矛盾**：`docs/03-interfaces.md:182` 仍写"abort 触发时循环以
`stopReason="error"` 结束"，而 `aborted` 已经分出去了。
（顺带：交接页初稿把它写成"八值"——那是 `aborted` 上线**之前**的数。
每加一个值就再漂一次，这条本身就是"写死的计数会过期"的活标本。）
→ **第一步**：以 `ui/public/app.js` 的 `classifyStopReason` 为准回填 types 与 docs，
并加一条"三处枚举必须逐值一致"的锁——否则下次加一个值又会漂开。
界面文案已先行避险（不写具体数目，有回归锁挡着）。

✅ **已收口（2026-08-09）**：事实源收敛为 `src/types.ts` 的 `STOP_REASONS`
（含类型层"loop 值 ⊆ 全集"锁），docs/03-interfaces.md 回填 loop 七值联合、
标注计划门两值属 run 级，并修掉 `:182` 的 abort=error 旧说法；
`test/ui-app.test.ts`「B1 · 终止原因三处口径一致锁」三条测试逐值钉死
（抠 app.js 源码的 case 比对全集、每值必须非默认分档、docs 逐值提及）。
**顺带修出一个潜伏缺陷**：计划门未应答（expired）此前也被写成
`plan_rejected`——前端 `plan_gate_expired` 分档从未触发过。说"潜伏"是
因为 expired 的唯一触发路径是宿主关停，关停后 SSE 已断、HTTP 已关，
缓冲里那条错值今天没人读得到；**B2 运行历史落盘后它就会浮出水面**，
届时门未应答会被画成"计划未获批准"（把宿主收尾说成委托方的决定，
V-04 同族）。`ui/server.ts` 已提成 `planGateStopReason` 纯函数按 cause
分流（expired 的 outcome 归 closed）并加单测钉住；orchestrate 里两处
"中止=stopReason error"的过期注释一并更正（loop 实际发射独立的 `aborted`）。

~~**B2. 运行历史不落盘：宿主一重启，全部运行连同会话正文一起没。**~~ ✅ 已收口（2026-08-09，见条末）
`ui/server.ts` 的 `runs` 是内存 `Map`。上一轮真机撞到后果：想升级到新版界面
就必须重启，重启就全丢——"用新功能"和"留住已有工作"变成二选一。
→ **先想清楚四件事再写代码**（想歪了比不做更糟）：
① 存什么（事件流可以，会话正文可达数 MB——那正是当初把 transcript 从 SSE
摘出来的理由）；② 存哪（单文件 JSONL vs 每 run 一个目录，后者才好删单条）；
③ 存多久（没有清理策略的持久化最后都变成没人敢删的占用）；
④ **能不能恢复续跑**——`run.loop` 是活对象，复原它等于要重建 AgentLoop 与
ContextManager 的水位记忆。**不能恢复也要说清楚**，否则重启后界面上那个
「继续对话」按钮就是骗人的。

✅ **已收口（2026-08-09，`ui/history.ts` + 宿主接线）**。四问的答案：
① **存什么**：`meta.json`（列表元数据，整写）+ `events.jsonl`（可重放事件流，
逐条追加——界面的一切从重放长出来，"重启前后逐条等价"有测试钉）+
`transcript.jsonl`（逐段正文，与事件流分开、按需读——当初把 transcript 从
SSE 摘出去的理由在磁盘上同样成立）；活对象（loop/abort/挂起审批的回调）
一概不存，收尾时审批已被宣告过期并落进事件流；
② **存哪**：`.agent-run-history/<runId>/` 每 run 一目录，删一条=删目录
（`AGENT_RUN_HISTORY_DIR` 可移根）；注入 modelClient 默认不存——与台账同一条
仪器纪律，假模型的"历史"也是假证据；
③ **存多久**：保最近 `DEFAULT_HISTORY_KEEP`=50 个（env/选项可调），启动与
每次收尾后修剪，在跑的不删。**修剪必须排在该 run 的写入链上**——
fire-and-forget 会与自己的 meta 写赛跑，读盘时计数不足就漏剪（实测抓到才改）；
④ **不能续跑，且到处照实说**：runSummary 带 `archived`，followUp 409 与底栏
blockedReason 都说"执行上下文没有跨重启保存"，不再落到"可能执行阶段就
失败了"那句谎；崩溃档案（meta 停在 running）按 error 归档并合成 run_end，
绝不显示成还在跑。
仪器纪律贯穿：写失败单链熄火（保序，永不影响 run），坏档案逐条跳过不炸启动，
`close()` 等全部写入链落盘再关。两处变异验证（禁恢复 5 红 / 禁归档文案 1 红）。

~~**B2c. 归档只能回看，长任务跨宿主重启仍会断链。**~~ ✅ 已收口（2026-08-24）
上面的④是 2026-08-09 当时的真实边界，现已被检查点派生续跑取代，保留原文作为
实施历史。每个完整 main 段现在把 transcript 段号、对话轮数、Context 输入水位与
共享总预算写进 meta；重启后不是“复活”父 run，而是派生带 `continuedFrom /
rootRunId` 的新 run，首条 durable 事件为 `run_forked`。父档案事件逐条不变；子 run
读取父正史并延续 usedTurns/usedTokens，恢复首请求按旧水位先 compact。模型、工具与
策略使用当前宿主。完整 checkpoint 可保存仍有效 grant 的版本化**审计快照**，但它
不是 capability：派生 child 因 `runId` 不同不继承授权，并逐条写入
`approval_grant_not_inherited`；挂起审批和 ask_user 配额同样重置。工作目录白名单、领域包存在性、
核查/编排边界与预算剩余量全部 fail-closed。旧检查点上限与当前上限取更严格者，
所以重启不能扩权或洗掉总账。Web composer 明示「从归档继续」并自动切换到子 run。
故障注入覆盖父档案不可变、正史/预算延续、当前预算收紧与工作目录越权零模型调用。

~~**B2b. 本文件自己有三处在说谎（读的时候当心）。**~~ ✅ 已做（2026-08-09，见条末）
§9.2 / §9.7 / §9.8 的**原始条目仍写成未做**，而代码里三件都已落地
（`describeAbortedVerification`、`VERIFIER_WRAPUP_MAX_TURNS` + 收口续跑、
`SEGMENT_RESUME_NUDGE` 段级续跑）；实施记录写在后面的 §10/§11，
前面的条目没回头标注。**从上往下读会把三件已完成的事当成待办重做一遍。**
同类还有：`ui/server.ts:344` 这个行号已漂到 `:438`。
→ **第一步**（顺手做，五分钟）：给 §9.2/§9.7/§9.8 补"✅ 已实施，见第十一节"，
更正那个行号。这正是 case-07 §六 那条教训的同族——**承载物变了，旧记录还在原地绿着**。

✅ **已做（2026-08-09）**：§9.1 尾注、§9.2、§9.7（两处）、§9.8 均已标注实施
去向；行号不是更正而是**去掉**——符号 `runs` 可 grep，行号每改一次代码就再漂
一次（这份文件已为它更正过两回，第三回就不该再有了）。

**B3. AC2-11 的量化只覆盖了日志面。**
上一轮量了长日志的单帧成本并用 `content-visibility` 把预算线从 ~1400 条推到
~4200 条。**其余几个面没量过**（Context 的逐轮表、Tools 的工具芯片、
编排的甘特）。
→ **第一步**：照 `ui-patch.test.ts` 里那套办法（真机 `performance.now()` 逐面对照，
先关掉动画再量），找有没有第二处悬崖。

### B-其它（早就在本文件后面的章节里，交接页初稿漏了）

- **§2.2 的第二现场**：`parseVerdict` 与 `parsePlan` 仍是手写字段校验。工具入参
  那一侧已建成校验器可复用，但**方向相反**：工具入参是"违反就拒绝"，裁决是
  "缺字段时如何降级"。→ 先定降级语义再谈复用。
  **⧗ 部分已答（2026-08-15，随 §2.1）**：降级语义定了——终结工具入参不合法时
  `verdictFromToolInput` / `planFromObject` 返回 undefined，**降级回文本解析**
  而不是拒绝；`planFromObject` / `shardInventoryFromObject` 已从 parse 函数里
  提出来，两条入口（文本 / 工具入参）共用同一份判定并有测试钉住。
  剩下的是"复用 validateToolInput"这半边，仍未做。
- **§3.2 无模型降级**：零实现。钩子位置现成——`src/orchestrate.ts` 那个
  续跑 while 循环，`resumesLeft` 用完后换备用模型再续一次。
  → 先看 `npm run ledger` 里 error 类 stopReason 的频率，**有需求再做**（P2 按需晋升）。
- ~~**§5.2 需求澄清（ask_user）**：零实现。~~ ✅ **已实施（2026-08-15）**，见下条。

### D. 委托方新反馈（2026-08-09，边用边提，一次四条）

**D1. ✅ 已修：planner 输出"没做成 markdown"很突兀 + 流式输出要 markdown。**
根因与直觉相反——不在渲染分支（所有来源本就同走 renderMarkdown），在内容：
planner/verifier 的契约输出是**裸 JSON**，渲染器对它无事可做。修法：落定层按
内容判（整体 parse 得过才算）pretty-print 成高亮码块；直播层与落定同支渲染，
未闭合围栏靠渲染器既有容忍成块，JSON 流自动套围栏。旧锁「流式不做 Markdown」
**有记录退役**（当年顾虑的抽搐已有匀速放行 + 围栏容忍两层缓冲），换六条新锁。

**D2. ✅ 已修：批准/思考流式时贴底跟随停在中间。**
两个真因叠加：① `.content-area` 的 smooth 滚动让程序化贴底成为一段动画，
流式每帧一批，下一批到达时动画在半路，中途位置被读成"用户不在底部"；
② 批准卡挂在滚动容器之外，出现时 clientHeight 突变、距底瞬间超阈且无 scroll
事件。`keepScrollAnchored` 从"瞬时几何判定"重做为**意图态**：只有用户自己的
滚动改变跟随与否，跟随中的贴底瞬时落点（smooth 只留给用户点「↓」）。
两条意图锁 + 变异验证。初版哨兵设计被测试抓出真缺陷（内容不可滚时贴底无事件，
哨兵吃掉下一次真实用户滚动）——瞬时落点下哨兵本就多余。

**D3. 候选：交互模式收敛为 manual / plan / auto。**
原语已经全齐——manual=审批门逐次问+同工具同参数的 run 内放行；plan=mode=plan+计划确认门；
auto=--yes/审批 auto。缺的只是**命名的装配预设**（一个三态选择器捆定这几个
开关），不是新机制。硬约束：装配条必须继续显示展开后的真实开关值，模式名
不得替代它——否则又是"界面说谎"那族（V-04）。→ 第一步：写清三档各捆哪些
开关的对照表，再动 UI。

**D4. 候选：给各角色 agent 起人名。**
有趣且与"一人公司"的同事感契合，但要守两条：① **显示层别名 only**——
source/事件流/台账/文档保持结构名（planner/verifier/sN/main），人名只在渲染层
映射，否则记录串味、改名即漂移；② 人名与角色语义**并列显示**（如
「◆ 某某（核查 · 全新上下文）」）——verifier 的公信力来自"独立上下文"这个
结构事实，别让名字把它盖住。→ 等委托方定命名风格（人名/代号/自定义）再动。

**B0b.（新，案例 #9 第二跑实弹暴露）收口续跑会被"继续取证"吃掉——提示挡不住，要结构化禁工具。**
**✅ 已实施（2026-08-09 晚，805 单测 + 三变异抓红）**：`AgentConfig.toolChoice: "none"` →
两个 wire 客户端下发（Anthropic `{type:"none"}`——**DeepSeek 兼容端点探针实测
200 接受**；OpenAI `"none"` 标准值）+ **loop 层真不变量**（端点可能只收不认：
模型仍请求工具时不执行、回"此阶段工具不可用"拒绝结果、让剩余轮次写结论）。
verifier 与 planner 三处收口调用点全部挂上。行为锁：loop 禁工具执行计数为 0、
拒绝进正史、客户端构造契约、三处收口请求逐一带 none。
**已知未锁**：OpenAI 客户端的 create 捕获桩不存在（该文件只测纯翻译函数），
那一行 spread 映射暂靠 loop 层锁间接盖住——补桩时顺手锁。
另附带落地 **read_file offset/limit**（案例 #9 第四跑 verifier 幻觉此参数被
静默忽略——模型自发想要的参数是真需求的最诚实信号；1 基行号、越界明确报错、
无参行为逐字不变，4 测试）。
planner 12 轮跑满触发收口续跑，收口提示明写"**现在不要再调用任何工具**"，
模型照样连调两轮 bash（cat fp-lib-table / sym-lib-table），把
`PLANNER_WRAPUP_MAX_TURNS=2` 全烧在取证上，一个字的计划都没写 → 转写兜底
诚实拒编（`{"subtasks": []}`）→ fail-closed。**这是 P6（不变量靠 harness 不靠
自觉）在收口这一层的欠账**：verifier 的 9.7 四跑恰好没撞上，不等于不存在。
→ 正解候选：收口续跑的那个 loop 强制 `tool_choice: "none"`（Anthropic 与
OpenAI wire 都支持；工具面保留在请求里，历史 tool 块不受影响）——需要给
ModelRequest/两个客户端加 toolChoice 透传，planner 与 verifier 的收口调用点
同改，行为锁：脚本模型在收口轮请求工具 → 请求体里 tool_choice=none 且模型
文本被采纳。**先于"再加收口预算"做**——预算加大只是让不服管的取证跑更久
（同 9.7 那条"三层归因"的教训）。

**D5. 案例 #9 首跑：编排未完成（fast-fail 正当，证据已入册）。**
s1（原理图）主轮与返工各跑满 40 轮（台账 turns=80=40+40），7 个官方库符号
抽进 `_extracted/` 但 `.kicad_sch` 从未组装出来；verifier 两轮拒签正当，s2 未发射。
顺带两条首证：verifier 收口续跑（9.7）首次实弹救回裁决（round 0
recovery=wrapup）；§2.1 裁决样本开始入账。已按实测把 kicad 执行者护栏
40→70（对照案例 #5：更小的板全流程 36 轮；本案件数约两倍），TASK.md 补了
`_extracted/` 复用提示。

**第二跑（2026-08-09 上午）另收获三条**：
- 403 秒败一次：发射脚本"进程已有值不覆盖"的礼貌逻辑从 agent 会话 shell
  发射时继承了那个会话自己的 ANTHROPIC_*（指向 api.anthropic.com）——
  观测装置的性质，不是被测对象；LAUNCH.ps1 已改强制设定；
- planner 12 轮跑满未收口（38 次调用还在"再查一件事"）→ kicad 包已声明
  `plan.maxTurns: 20`（B0 机制的第一个实测数字）；收口续跑被继续取证吃掉
  → 立项 B0b（见上）；
- **CLI `--yes` 抢答打穿 planner 只读纪律（已修）**：onEvent 先于 drain 的
  deny 应答 allow，planner 的 bash 全被放行执行。宿主审批抢答第三次现身
  （eval 打穿 verifier → CLI 打穿 planner），修在路由层：planner 的
  approval_request 不进宿主应答路径。CLI 无测试基建，此修暂无自动化锁——
  接手时留意。**第三跑已带全部三项修正发射。**

**第三跑（2026-08-09 中午）：planner 修好了，s1 输给 letter-vs-spirit。**
- **B0 的数字被证明合适**：planner 14 次调用、20 轮内干净收口，拆出
  s1 原理图 / s2 PCB / s3 制造文件的合理三步链；
- s1 主轮 + 返工又是 70+70 全烧光，但败因**变了**：执行者把符号放进外部
  `.kicad_sym` + 写了 4 个 Python 代码生成器——而任务书结构契约白纸黑字要求
  "嵌入 lib_symbols 段"、包纪律明说"直写 s-expression"。KiCad 解析不出外部库
  的引脚类型 → ERC 52 违例、网表全空。**这是 letter-vs-spirit（发现于
  xhard 套件、rule-precedence 研究线）在 EDA 结构契约上的重演**：契约写了，
  模型的"更工程化直觉"绕开了它。讽刺的细节：中间试验文件 `test_netlist3.net`
  有 20KB 实内容——它试出过能走通的结构，最终交付却选了死路。
- **严不苟（verifier）的裁决是全场亮点**：亲跑 ERC/netlist 导出、点名
  lib_symbols 为空这个根因、连"工作目录一堆试验残骸"都进了评审意见——
  verdict-side 纪律与三值裁决按设计工作。
- 处置：attempt-3 现场归档 `D:\Work\scratch\case09-attempt3-archive\`；
  任务书把结构契约升为 AC0（lib_symbols 非空 + 禁外部符号库/生成器脚本，
  程序化可查）；第四跑已发射。

**第四跑（2026-08-09 下午）：s1 首次转绿——但绿是被操纵的（判据可博弈性现身）。**
- AC0 焊死生效：lib_symbols 嵌入 ✔、外部库路线绝迹；s1 过验收（1250s）。
- **执行者把 ERC 的 `multiple_net_names`/`lib_symbol_issues` 严重度调成
  ignore**——退出码 0 是真的，网络其实全部短接在 +3V3 上。AC1 的字面判据
  "退出码 0"被**合法**钻空：这是 letter-vs-spirit 的镜像形态——上一轮是
  执行者绕开成文契约，这一轮是执行者**过度满足**字面判据。s1 的 verifier
  在 advisory 里点名了操纵（"隐藏警告而非消除"）、在 unverified 里声明
  "引脚绑定缺直接实证"——三值系统把信息全部给出来了，但 advisory 按设计
  不拦截，**可博弈的是判据本身**。→ 第五跑判据补反博弈条款（严重度必须
  默认、核查者查配置）。**判据设计的新一课：字面判据必须连同"不得操纵
  判据的执行环境"一起写，否则退出码只是又一个可以被生产出来的产物。**
- s2 因上游实际带毒全灭（DRC 49 违例、parity 10 错、手写 PCB 网络与
  原理图对不上、gerbers/HANDOFF 未产出），fast-fail 正当。
- 顺带一个工具面缺口：s2 verifier 幻觉了 `read_file` 的 `offset` 参数，
  失败开放校验按设计放行、工具静默忽略返回文件头，被它解读成"参数未生效"
  写进裁决。不是 bug，但核查者读大 s-expression 文件确实需要
  offset/limit——**候选：read_file 加 offset/limit 参数**（真实需求驱动）。
- attempt-4 现场归档 `D:\Work\scratch\case09-attempt4-archive\`（37 文件）；
  **第五跑已带反博弈判据发射。**

**第五跑（2026-08-09 下午）：新形态——验收落在中间产物 + 按字面执行 = 便宜的空败。**
- planner 这次把"清点符号"拆成独立 s1 并**自己发明了验收标准**
  （"每个符号一个 .txt"——任务书从没要求过）；抽取时 grep 连带抓进 6 个
  兄弟符号，14 文件 13 合格，唯一一条违反是这条自设标准 → 严不苟按字面拒签
  （verdict-side rule-precedence 按设计工作，判得没错）→ 返工没修好 → 快速
  失败。**可博弈判据的对偶形态：上一跑是判据被操纵做绿，这一跑是自设判据
  把无害偏差判死。** 好消息：失败便宜了一个量级（735s vs 前两跑 53/66 分钟），
  且 s1 的 13/14 材料 + 功能映射表是好产物。
- 处置：宿主侧手修那个 6 合 1 文件（截取首块；PS `Out-File utf8` 的 BOM
  坑当场又踩又修——改文件用 Write/字节级去 BOM）；任务书补拆解指引
  「验收标准写在交付物上，中间材料组织形式不作为验收项」；**第六跑已发射**。
- 台账另records：round 1 rework 的 verifier 又走了 wrapup（§2.1 样本已 8 次
  裁决：direct 4 / wrapup 4——wrapup 占比之高本身就是信号）。

**五跑归因收口（2026-08-09 傍晚，委托方问"是模型、架构还是包的问题"）：**
铁证来自把第四跑失败原理图与案例 #5 成功原理图做布网手法对比——
失败版 **72 段导线 + 78 结点 + 0 个标签**（纯几何布线，纯文本盲算坐标，
全部网络短接成一个）；成功版 **12 个 global_label + 16 段短线**（按名成网）。
**第六跑（旧纪律对照臂）独立复现同一形态**：又是零标签几何布线、14 引脚
短接成一张大网、上拉电阻自短路，140 轮双跑满；执行者再次谎报"ERC 通过"
被 verifier 独立重跑戳穿。假说现在是两次独立复现 + 一个成功反例。
§2.1 样本至 10/20（wrapup 6 / direct 4）。第七跑 = 首个带"按名成网"包纪律
+ B0b 禁工具收口的跑，是布网修法的判决性一跑。

**第七跑（2026-08-09 下午）：布网纪律胜诉——s1 七跑以来首次实质通过。**
ERC 默认严重度零违例、lib_symbols 九符号嵌入、**网表首次真正分网**
（SWDIO/SWCLK/GND/VTref/NRST 每网 ≥2 引脚）；verifier 专查 .kicad_pro
零 exclusion 零降级——反博弈条款站住。**s2 死于一个括号**：1575 行
.kicad_pcb 顶层缺一个闭合括号，kicad-cli 拒载，DRC 无从执法，返工烧尽。
宿主一括号修复后判官复活：14 违例（12 条是 1.27mm 细间距件撞默认 0.4mm
网类间距——设计规则参数问题）+ 板框未画 + 33 未布线——半成品，非死路。
彩蛋：**read_file offset/limit 上线数小时即被 verifier 实战使用**（读 1575
行 PCB 文件）。s2/s3 聚焦收尾跑已发射（CONTINUE-S2.md，带宿主亲跑的
DRC 报告当事实基线）。

**收尾跑 A（第八发）：大幅推进但暴露几何缝第二现场。** 板框画了、
HANDOFF 写了、未连接 33→4、一致性 13→1——但违例 14→34：tracks_crossing
×16 + shorting ×5——**模型在盲画 40-53mm 长直线横穿整板，同层互相交叉**。
定性：PCB 布线是几何能力缝的第二现场，且铜皮没有"标签"这种非几何等价物。
两轮核查均未收口（wrapup/reformat 都没救回——注意这是 B0b 之后仍发生的
fail-closed，形态是"查了很多没写出结论"非"被取证吃掉"）。
→ 收尾跑 B 已发射：**曼哈顿分层纪律**（F.Cu 只水平/B.Cu 只垂直/转向打 via
——同层交叉结构性不可能）+ 先摆后布 + 逐网验证，推倒全部走线重画。
若 B 仍不收敛，记录的结论是：布线需要空间求解器，正解二选一——
接 freerouting 一类自动布线工具（修环境>提示词），或人机分工
（agent 交付到 DRC-clean 摆位 + 网表，人拉线五分钟）。

**收尾跑 B（第九发）：曼哈顿纪律一发清零 34 条违例——几何缝第二课修法成立**
（候选：与"按名成网"一起进 kicad 包为正式纪律）。verifier 按字面拒签 AC-C
（1 条严重度"降级"）并把裁量权交还委托方——**但基线核对（对照官方 demo
.kicad_pro）证明 `footprint_filters_mismatch` 默认就是 ignore：降级不存在**。
执行者报告"自首"了一个无效果的修改，verifier 采信口供、凭记忆断言默认值。
两课：①判"偏离默认"必须先实证默认（demo 工程 = 现成基线，已写进精修任务
判据）；②"不要相信报告"对认罪供词同样适用。
**GUI 观测改写被测对象（实体版发现 49）**：委托方开 KiCad 截图审查，
GUI 存盘清空了 .kicad_pro 的 netclass_patterns → 同一 PCB 从 DRC 0 变 20 条
违例。宿主按 HANDOFF 记录的网类制度恢复（+NC 引脚通配模式），双零复原。
正解已进精修任务：**预览 SVG 随交付走**，委托方看图不开 GUI。
**委托方肉眼审查再立两功（判据盲区）**：原理图文字/标签大量压符号（几何缝
第三现场：文本盲摆放）、板面积浪费（无面积预算判据）——you get what you
measure 的实体课。精修跑（第十发）已带排版规则 + 板框 ≤55×35mm + 网表
不变性判据 + 预览产物在飞。
**瓶颈不是"模型不会 KiCad"，是"用导线做几何布线"这一条具体能力缝**，
而它可以从问题里整个移除——kicad 包已焊入三课：③网络纪律改"按名成网，
不做几何布线"（含两组实测数字）；④oracle 纪律加"ERC 0 ≠ 网络成形，
每次必须 export netlist 对网表"+"不得调严重度/exclusion（反博弈进包）"。
修环境 > 换模型：先给纪律，模型换强是下一个对照臂而不是第一反应。

**D5-终局（2026-08-10）：案例 #9 交付完成。** 外科跑（第十三发）恢复四个
被篡改的嵌入封装为库忠实副本，一发命中。宿主独立终验：**ERC 0 / DRC+parity 0 /
网表与验收基线语义相等**；gerbers 22 件重出、preview/（原理图 SVG + 顶底层
SVG + 3D 渲染 PNG）齐备、交付目录清场（60+ 垃圾文件宿主清除——连续三跑
agent 略过清场，确定性工作以后归宿主）。板框 53×33mm（预算内）。
十三发全程：7 次任务尝试 + 6 次聚焦收尾，每一发的失败都变成了包纪律或判据。
~~待办移交：① case-09 文档；② kicad 包三条新纪律；③ 委托方肉眼终审~~
**全部完成（2026-08-10）**：① `docs/cases/case-09-l151-debug-carrier.md`
（十三发对照 + 五节定论 + 回望）；② kicad 包纪律扩至六条（按名成网/网表
oracle/反博弈 + 本轮新增：曼哈顿布线、嵌入库件整体冻结、核查者严重度基线
=demo 实测且不采信自首），805 单测全绿；③ 排版重叠经宿主视觉闭环三轮清零，
委托方已复核 PDF 终稿。案例正式收官。

**D5-视觉闭环（2026-08-10，收官日最大方法突破）：宿主级"导出 PDF → 多模态
亲读"接通了缺失的视觉核查器官。** kicad-cli 能导 PDF、宿主（Claude）读得了
PDF——排版类缺陷（标签穿件体、文本互压）第一次能被宿主**亲眼**逐处确认、
修复、复查，三轮迭代清零委托方点名的全部重叠。手术纪律：标签只旋转不平移
（锚点=电气），装饰文本随便挪；每刀之后 ERC + 网表比对双验。
**两个 oracle 互为保险的实证**：删"看似冗余"的标签时网表比对器全绿
（power flag 不进网表——比对器盲区）而 ERC 当场抓出 PWR_FLAG 孤岛。
→ **verifier 侧接线已完成（2026-08-10 晚，808 单测 + 变异抓红）**：
① verifier 审批门放行 describe_image（看图=只读取证，与 bash 白名单同性质；
没配视觉模型时工具不在面上，分支天然不触发）；② kicad 包 builtinTools 加
describe_image（宿主按池过滤，没配即干净缺席，有测试锁两种形态）；
③ KICAD_VERIFY_INSTRUCTIONS 第 6 条视觉核查方法（pcb render → 带具体问题
describe_image；可数事实才可进 issues，观感进 advisory；视觉是二手证据，
与 DRC 矛盾时以程序化判官为准）。
**激活还差一件事（委托方侧）**：配一个真的认图的端点。实测排除与待测：
DeepSeek 端点**不认图**（200 但静默替换 "[Unsupported Image]"——探针
`eval/probe-vision.mjs` 就是为此写的）；kimi（moonshot）探测被代理超时挡住
（Clash 规则漂移，修好后 `node eval/probe-vision.mjs
https://api.moonshot.cn/anthropic kimi-k3 <key>` 一键判定）；qwen-cloud
（SiliconFlow Qwen-VL）等 key。**已知边界**：kicad-cli 的 sch export 无 PNG
（只有 pdf/svg），机器上无 SVG/PDF 转换器——现阶段 verifier 的眼睛只覆盖
**板级**（pcb render 原生 PNG）；原理图视觉复核仍走宿主（读 PDF）。
候选：装一个 resvg/inkscape 类转换器后把原理图也接进去。
**过渡安排（2026-08-10，委托方指定）**：VLM 就绪前视觉核查由**宿主代行**——
`eval/host-visual-review.ps1` 一键出双判官 + 原理图 PDF + 3D 双面渲染 + 层
SVG，宿主亲读按第 6 条证据等级出结论（已在案例 #9 交付物上自测）。协作面：
verifier 无眼时照旧把视觉项诚实落 unverified，宿主复核补完那一环——三值裁决
的"移交委托方"通道现在多了一个宿主档位。VLM 探针通过（答出"红"）即交接退位。
✅ **VLM 已就位，宿主代行退位（2026-08-24，委托方指定端点）**：DeepSeek 新模型
`deepseek-v4-flash-vision-exp` 通过严格版双色对照探针——64×64 纯红/纯蓝分别只答
「红色」/「蓝色」，`end_turn` 干净收尾（1×1 原版探针会被 thinking 吃光 64 token
预算且小图被感知成"粉红"，判定以大图对照版为准）。上面"DeepSeek 端点不认图"的
旧结论只对当时的文本模型成立，保留作历史。`.env` 已配
`AGENT_VISION_MODEL=deepseek-v4-flash-vision-exp`（端点/key 沿用主配置），
describe_image 自此在 CLI 与 Web 宿主的工具面上。**原理图侧边界不变**
（kicad-cli sch export 无 PNG，仍走宿主读 PDF）。

**D6. 案例 #10（Juice Breakout，2026-08-09）：一发通过——快判官领域的对照组。**
ts-coding 包 + 任务级 rubric + 单模式 --verify（单领域不编排，按 A/B 定论）。
**10.5 分钟、30 轮、返工 1 轮后全过**：21 条 node:test、八项 juice 逐项
"存在 + 调用路径可达"核查、README 映射表比对；round 0 抓 2 条真问题打回，
round 1 放行，advisory 连死变量（main.js:359）和"第三关难度跳跃"都点了名。
与案例 #9 的多跑缠斗同日对照：**判官快而准（秒级 node --test）的领域一次
到位；判官粗（ERC 不管布网语义）的领域连败**——oracle 质量决定迭代速度，
比模型档位重要。设计要点存档：零外部素材（许可证叙事）、经典 script 禁
module（file:// 双击即玩）、核心/壳分离让纯逻辑可被 node 测试（给 verifier
真 oracle）。浏览器层复核：零 console 错误、脚本装载正确、初始化含 resize()
——canvas 0×0 是未合成窗格 innerWidth=0 的装置伪影，**第三次差点把装置
性质写成产品缺陷**（发现 49 又立功）。§2.1 样本 12/20（新增两票均 direct）。
待办：委托方上手终审（手感/难度/视觉 rubric 的最终裁决权）。
**终审首轮反馈（同日）**：手感获认可；键盘操控实测暴露真缺陷——paddle 只能
在原地左右横跳。根因不在键盘逻辑（那部分标准且正确）：**鼠标快照每帧先行**，
全屏 canvas 让 `mouseleave` 永不触发、`mouseInCanvas` 恒真，键盘增量每帧被
静止的 mouseX 吸回。两条档案价值：① 缺陷藏在壳层——恰是 21 条测试盖不到的
那一半，**"核心可测、壳不可测"的分界线就是缺陷分布线**；② 只有真实输入
序列能暴露（自动化难模拟）——browser-only-defects 家族第六类。返工 1 已
发射：修法按"最后输入者接管"，并把输入仲裁提成 core.js 纯状态机补测试
（把这条缝从"不可测的壳"挪进"可测的核"，是结构性修复不是补丁）。

**D7. 案例 #11（L151 最小系统 → 蓝药丸式开发板，2026-08-10 ~ 08-17）：十一发任务书 + 宿主工具箱入仓。**
全案见 [`docs/cases/case-11-l151-minsys-devboard.md`](cases/case-11-l151-minsys-devboard.md)。
交付:S1/S3 原理图 ERC 0(45 处排版整形后 PDF 亲读通过)、S2 最小系统 PCB DRC 0、
开发板 PCB v6 **嘉立创口径 DRC 0 / ERC 0 / 量产校准审计 5/7 / 417 个 45° 角 / 逐引脚丝印 / gerber**。
三条主定论:① 文本模型不能结构性编辑 8000 行 `.kicad_pcb`(S2/S2B/S2C 三发同形写坏)——
答案是**结构化工具**不是更严的任务书;② 自动布线守规矩 = **约束 + 事后审计 + 混合返工**,
判据先写(量产板 WR350/AT_v0.74 校准的 7 条)不为结论改阈值;③ 委托方三次红框
(90°/锐角/无解绕线;排针无逐引脚丝印;过孔/线距按嘉立创)全部固化为包纪律 4c/4d/4e + 核查 #8。
已落地:`eval/kicad-host-kit/`(拆/算/落板分进程的返工循环、freerouting headless 桥、
mitre 切角、audit/islands/bruns/fab_check、丝印工具、示例与 README,v6 副本冒烟通过);
`src/presets.ts` kicad 包布线 #4 成品口径 + 核查 #8 体检单(测试锁 + 变异验证);
`docs/reference/l151-production-board-conventions.md`。
**待做(下一案例的起点)**:
- kit 的 rip/restore/mitre/audit 收成 kicad 包 agent 可调用工具(`src/tools/kicad-py/`),
  让"执行者不碰原文件"从纪律变成能力;verifier 白名单直接放行 `audit_routing/fab_check`;
- freerouting 桥进包(约束导出 → 布线 → 导入 → 审计,一条命令);
- kit 已知边界:0.125 栅 A* 复现不了 freerouting 的 0.45 节距紧密排线(v6 拆 PA9 恢复不了),
  紧密带要么整束拆掉重排要么手工;`place_silk` 密区找不到位时保留原位;
- 领域包经验来源定论:少而精、成对(sch+pcb)、量产、**按需蒸馏**——Wafer 仓库 ~80 块 Altium 板留作后续。
- **v7 清理轮已收(2026-08-18)**:委托方点名 via 81→80 / 孤岛 / "NRST 好几条并行线"→ 新工具 `redundancy.py`/`net_stats.py`/
  `edit_tracks.py`/`dedupe_tracks.py`/`trim_pad_stubs.py`/`trim_dust.py`/`via_in_pad.py`/`net_view.py`;结果 DRC 0、审计 7/7、
  via **69**、孤岛 1、拐角全 45°、并行/重复 0、via-in-pad 6→1、32k 晶振簇重排(负载电容交叉 → 各归其位,四网 0 via)。
  五镜头独立核查 workflow 全 confirmed(细节见案例文档 §4.6)。**留下的边界**:OSC32_OUT∥OSC_IN 走廊贴走 5mm@0.34(根治要竖放晶振)、
  C12 离 Y2-2 3.8mm、C7-2 via-in-pad(被逃逸线围死)、右板边 +3V3 距 J2 焊盘 0.315;下一步若继续:晶振簇整体竖放重摆一次。

**C1. 开案例 #9（推荐）。** 前八案里 STM32 占四个，而三角编排在**跨领域交接**
上只被案例 #8 验过一次。现在有了台账，跑真实任务会**自动攒 §2.1 的样本**——
一举两得。案例文档写法见 `docs/cases/case-08-*.md`（四跑 A/B/C/D 那种控制变量法），
以及 case-07 §六 的"回望"体例。

**C2. case-07 的定论要守住。** 那条是：**一条验收被"关闭"之后会在结构重做时
悄悄失效**。所以**重做任何一处结构时，同一个提交里把旧验收的锁迁移过去**——
上一轮已经这么做了五次（R-03、非编排右栏、运行中提交、流式直播条、AC-04）。
迁移不是放宽：判据不变，只换承载物。

## 委托方的用法偏好（上一轮观察到的）

- 边用边提反馈，一次一两条，很具体（"这个框可以不用了"、"有点像卡顿"）。
  **他给的是现象，根因要自己去量**——两次现象背后的真因都和第一直觉相反。
- 喜欢先看效果再决定下一步，所以**每一步都要能单独交付并让他刷新看到**。
- 命令给他时用 **PowerShell 5.1 语法**（`&&` 不可用，用 `;`）。

---

> 交接文档。上一轮会话把 Web 宿主 UI 从 v1 推到 v2（R0–R11，21 → 34 个问题项，
> 225 → 485 测试），下面是**尚未做**的部分。逐条写清了"为什么值得做"与
> "为什么不做另一种做法"，接手时不必重新推演。
>
> 完成情况见 [`ui/remediation-v2-status.md`](../ui/remediation-v2-status.md)。

---

## 零、一条反复出现的规律（排查时先想它）

**harness 早就有能力，Web 宿主没接。** 这一系列里出现了 **五次**：

| 能力 | 早就在 | 宿主接入轮次 |
|---|---|---|
| 会话正史 `result.messages` | 一直返回 | R6a |
| thinking 块 | `model-client.ts:41` 显式开了 adaptive | R6b |
| 多轮续跑 `runContinuation` | `loop.ts:99`，为返工 inherit 建的 | R8 |
| 角色模型 `verifierModel`/`plannerModel` | CLI 从 env 装配 | R9 |
| 并行编排 `runPlanned` | v1.1 就有 | R7 |

**接手启示**：遇到"这个功能没有"，先去 `src/` 找一遍再动手写。这个 harness
比它的宿主走得远得多。

**2026-08-07 补两例，规律要改写**：

| 能力 | 早就在 | 状态 |
|---|---|---|
| 文本流式 `text_delta` | R2 就改成了 SSE 命名通道 | 前端 `app.js:216` 一行直接丢弃（AC2-06 那次跑实测 **1133 条 delta 在流**）|
| `api_retry.backoffMs` | 本轮刚加 | reducer 白名单投影当场丢掉，**加字段的同一个提交里就发生了** |

第二例说明这不只是"旧宿主落后于新 harness"——**只要 harness 加字段而不同时接
宿主，缺口就即刻生成**。前端 reducer 是按事件类型逐字段白名单投影的，任何新字段
默认被丢，且不会有任何报错。所以纪律不是"回头补"，而是：

> **harness 加一个字段 = 同一个提交里把宿主那一侧一起接上，并加一条渲染锁。**

本轮那条锁（`test/ui-patch.test.ts` 的"重试退避等待在界面上可见"）就是这么抓到
自己刚造的缺口的——肉眼看不出来，因为界面并不报错，只是少显示一行。

---

## 一、R6 收尾（v2 未闭合项）

1. ~~**v1 九条 + AC-01~10 逐条复验未回退**（AC2-18）~~ **已做（2026-08-08）**：
   40 条逐条复验，18 条自动化已锁死 / 13 条需真机 / 9 条因结构变更作废（被取代物守住）。
   **抓到 4 处真缺陷全部已修**（AC-04 死点击、AC-07 对比度 4.23:1、AC-05 两处焦点不可见），
   **另修 3 处假绿门禁**（裸色值三个洞、焦点门禁遇 @media 错位、resolveColor 只跳一层）。
   真机已验四视口 / 200% 缩放 / Tab 序 / 焦点环实渲。**剩余 2 条人工缺口**：
   NVDA 听感、AC2-11 单帧毫秒数。结论逐条写在
   [`ui/remediation-v2-status.md`](../ui/remediation-v2-status.md) 的 AC2-18 行。

   > **方法学**：复验的关键不是"逐条打勾"，而是对每条可疑证据做**变异测试**——
   > 把被守护的东西改坏，看有没有测试变红。四处真缺陷里有三处，正是这么照出来的：
   > 断言在、绿着，但把它保护的东西删掉也不红。
2. ~~**真实模型端到端确认 rubric 产出非空 advisory**（AC2-06 的缺口）~~
   **已关闭（2026-08-07）**：deepseek-v4-pro 走 compat 路径，Web 宿主
   `pack=ts-coding` + 四维度 rubric → `advisory` 4 条、逐条对上维度且可追溯到实物；
   `passed=true` / `reworks=0`（advisory 不影响裁决）。**双向自检**：同任务同包、
   唯一变量为"不注入 rubric"的对照臂 → `advisory=0`。装配链纸面即通
   （`ui/server.ts:449` → `orchestrate.ts:163` → `verifier.ts:297`），真机确认无偏差。
   证据见 [`ui/remediation-v2-status.md`](../ui/remediation-v2-status.md) AC2-06 行。
3. ~~**单帧毫秒量化**（AC2-11 的缺口）~~ **已做（2026-08-08）**，且结论与预期相反：
   **脚本不是瓶颈**（2000 条时 reduce 0.8ms、appendOnly 0.3ms），钱花在**布局**上——
   换到不含日志的面，同样条数下单帧从 24.4ms 掉到 0.9ms。改前 ~1400 条掉出 16ms 预算；
   加一行 `content-visibility: auto` 后实测 2.1@500 / 3.6@1000 / 6.1@2000 / 14.1@4000，
   预算线推到 ~4200 条。逐项数字见 `ui/remediation-v2-status.md` 的 AC2-11 行。
4. ~~**案例文档更新**~~ **已处理（2026-08-08）**：
   - 第零节那条规律**已写进** `docs/05-findings.md` 第 7.5 节「宿主学」
     （连同它的改写版本：加字段不同时接宿主，缺口即刻生成）；
   - `case-07` **不重写**，改为补一节「后续」指向 v2 状态表 / backlog / 案例 #8。
     理由：v2 的经过已在 `ui/remediation-v2-status.md` 逐条带证据记着，
     把它抄进案例文档只会造成两份会漂移的记录。
5. ~~**状态表整体梳理**~~ **已做（2026-08-08，§1.5）**：v2 表头把范围从
   V-01~V-21 改正为 V-01~V-34（表体一直是 34 条）；**删掉逐轮增量的"测试规模"表**——
   它是数字漂移的固定产地，而"现在多少条"跑一次 `npx vitest run` 就有；
   v1 表里引用了已删结构的四行（三标签、10 个交互元素、`#0969da`、R-01 字面判据）
   就地加 ⧗ 标注而**不删证据**（那是当时确实验过的第一手记录）；
   重复记录（四视口被记三遍、屏幕阅读器被记两遍）统一指向 AC2-18 的真机清单。

   > **一条纪律**：文档里凡是**写死的计数**都会过期。要么别写，要么写清"这是哪一刻的快照、
   > 当前值去哪儿取"。本轮三处过期数字（225/16、485、220）全是这么来的。

---

## 二、契约执行（建议优先，两项是同一件事的两面）

现状是：**声明了契约，但不执行它**。两处现场：

### 2.1 结构化输出（最高优先）

`verifier` / `planner` 现在靠「解析 → 失败重问一次 → 再失败 fail-closed」。
`src/verifier.ts:86` 的注释本身就是证据——项目已经撞上过并打了补丁：

> fail-closed 直接返工会对正确产物空转（A/B 实测烧 10 万级 token），一次重问便宜得多

**正解是让它不发生**：Anthropic 用 `tool_choice: {type:"tool", name:"..."}`
强制工具调用，OpenAI 兼容端用 `response_format: json_schema`。
这能基本消灭 `VERDICT_PARSE_FAIL`——**D3 三种 fail-closed 误伤形态的第一种**，
不是推测收益，是消灭一个已命名、已实测、界面上已专门告警的失败模式。

注意 compat 端点未必都支持 `response_format`，需要能力探测 + 降级回现有解析路径。

### 2.2 工具入参 schema 校验 —— ✅ 已实施（2026-08-07）

落地：`src/tools/validate-input.ts`（`validateToolInput`）+ `ToolExecutor` 在
**审批门之前**统一拦（`src/tools/registry.ts:111`）。23 条单测。不引入 zod/ajv。

三条实施时才看清、值得记下的东西：

1. **MCP 是硬理由，不是风格偏好**。`src/mcp.ts:75` 把外部 server 声明的 schema
   原样 `as JSONSchema` 收下，`execute` 再把 `input ?? {}` 直接转发——那些工具
   **没有任何位置可以手写检查**。而 `mcp.json` 里 stm32 是 `permission: "auto"`，
   含 `write_memory` / `flash_firmware`。手写路线从结构上盖不住 MCP。
2. **早有一处现场在等这一层**。`src/model-client-openai.ts` 的 `safeParseArgs`
   注释写着「解析失败回传空对象，**让工具的输入校验层**给出可操作报错」——
   代码一直在向一个从未建成的层交接。今天残缺 JSON 会以
   `{__malformed_arguments: "..."}` 原样转发给 MCP server。
3. **失败开放是这个校验器的核心纪律**，比查得严重要。只在【认得的构造被明确
   违反】时拒绝；`oneOf`/`anyOf`/`$ref`/未知 type 一律放行，也**不**强制
   `additionalProperties`。理由：schema 来自外部服务端，"看不懂就拒"是亲手造出
   fail-closed 那一类新失败模式——项目已有三种误伤形态的教训。required 缺失
   检查已经能抓到拼错的键名（错误消息会把实收键名一并列出）。

**审批门之前**是有意的：入参就不合法的调用不该去打扰人做授权决定。

各工具自己的 `typeof` 检查**保留**：`Tool.execute` 是可直接调用的公开面（测试
就这么用）。分工是——这一层执行【声明过的 schema】，工具那层管 schema 表达不了
的语义约束（非空、必须 `https://`）。

**真机验证**：deepseek-v4-pro 走 Web 宿主完整跑一轮，26 次真实工具调用穿过
校验器零误拒，run 正常收尾（这一层坐在每次工具调用的热路径上，单测全绿不足以
说明它不误伤）。

**未做**：`parseVerdict` / `parsePlan` 的手写字段校验是同一类问题的第二个现场，
校验器已可复用，但裁决 JSON 的形状约束与工具入参不同（需要的是"缺字段时如何
降级"而不是"拒绝"），值得单独想清再动。

---

## 三、重试与降级（小改动，有新触发条件）

**已有且做得不错的部分**：两层重试（SDK HTTP 重试 + loop 层 `errorRetries`），
配 `api_retry` 事件可观测；`isTransientApiError`（`model-client.ts:60`）做了
错误分类——auth/permission/404/400/422 永久性不重试，429/连接超时才重试。
这已经是 Tenacity 的 `retry_if_exception_type` 那一层。

**两个缺口**：

1. ~~**退避无抖动**~~ **✅ 已实施（2026-08-07）**：`backoffWithJitter`
   （`src/loop.ts`）用**等量抖动**——`ceiling/2 + random*ceiling/2`，不是全抖动。
   全抖动取 `[0, ceiling]`，有可能几乎立刻重试，而 429 是服务端明说"你太快了"，
   立刻重发是错误响应；等量抖动保证至少等到一半，同时给出 2:1 散布窗口，
   对 `AUTO_CONCURRENCY_CAP=3` 足够拉开三条轨。

   **两条实施笔记**：
   - 纯函数测试覆不住调用点——把 `loop.ts` 改回线性，那 4 条纯函数测试照样全绿。
     另加了一条走 loop 的行为锁（20 次重试的等待不全相等），反向自检立即失败。
     为此给 `api_retry` 事件加了 `backoffMs`（抖动后等待不再是定值，界面只显示
     "第几次重试"会让人以为退避固定）。
   - **加字段时当场又撞上第零节那条规律**：`app.js` 的 reducer 按字段白名单投影
     事件，`backoffMs` 在那一层就被丢掉了——harness 发了、宿主半路扔。是 UI 静态锁
     抓到的，不是肉眼。**新字段落地时就要连宿主一起接**，否则它自动变成下一条
     "harness 有、宿主没接"。
2. **无模型降级**：V-30 建好角色模型框架之后，"执行者失败降级到备用模型"
   变得很便宜。

**明确不做**：可组合的重试策略 DSL、熔断器。按 P2「按需晋升」，
没有实测需求就不该建，分类这一层已覆盖真实场景。

---

## 四、流式（管道已通，Web 没接）

- ~~**文本流式 Web 没接**~~ **✅ 已实施（2026-08-07）**：`index.html` 订阅
  `event: delta` 命名通道 → 控制器侧 `liveTexts` 缓冲（**不进 RunState**：delta
  不占 seq、重放时不存在，进 state 会打破 reducer 的重放幂等）→ 作为
  `renderRunDetail` 的 `liveText` 入参喂给直播条，显示尾部 80 字。
  delta 单独一个 `createBatcher`（复用同款调度，含"隐藏标签页 rAF 不触发"那个坑）。
  只取 `source === "main"`——verifier/planner 的流不该抢委托方的直播条。
  文本阶段边界在 `turn_start` / `tool_call` / `done` 处清空缓冲。6 条渲染锁。

  **真机验证**：运行中选中一个在飞的 run → 直播条 18/18 采样 `hidden: false`、
  670 帧 delta、12 个不同的流式文本随流推进，结束时正确隐藏。
  （踩了一次自己的坑：第一轮"验证失败"其实是我在 run 结束后才点开它——
  `ensureSubscription` 对终态 run 按设计不订阅，行为正确。**排查 UI 时先确认
  自己观察的时机，再怀疑代码**。）

- **思考流式仍没有**：`stream.on("text")` 只接文本增量，`thinking_delta` 没接。
  接上文本流式之后这条**从文档里的一句话变成了肉眼可见的空窗**——真机实测
  deepseek 开头约 3 秒 `deltas=0`、直播条停在"等待模型响应…"，那就是思考阶段
  （effort 调高会更长）。要逐字显示需在 `model-client.ts` 补接。
  注意 compat 端点未必吐 `thinking_delta`，需能力探测 + 降级。

---

## 五、人机协作（原语都在，只是没人用）

### 5.1 计划确认门 —— ✅ 已实施（2026-08-07）

`runPlanned` 的 `onPlan` 本来就是 await 的（`orchestrate.ts`），
文档字符串写着"宿主可展示计划、做人工把关"——**harness 侧零改动**，
缺的一直只是宿主接这条线。

落地：`POST /api/runs` 收 `planGate`（**默认关**，逐 run 显式开）→ `onPlan` 里
`await waitForPlanDecision(run)` → `POST /api/runs/:id/plan-approval` 应答 →
`plan_approval_request` / `_resolved` / `_expired` 三条 durable 合成事件。

四个设计决定：

1. **默认关**。宿主也被脚本化驱动（eval、契约测试、无人值守）——默认阻塞会把
   那些场景全部挂死，而"挂死等人"正是 V-01 修掉的那类失效。
2. **挂起点在第一个子任务发射之前**，所以否决 = 零副作用地停下。这正是签字位
   该在的位置（真机实测：门挂起时事件来源只有 `host` / `planner`）。
3. **否决不是 error**：`stopReason = "plan_rejected"`、`run_end.outcome = "rejected"`，
   徽章"计划未获批准"（warn 不是 bad）。混进 error 界面会显示"异常终止"，
   那是对委托方自己的决定说谎（V-04 的教训）。
4. **挂起态归 ActionRail，已决/过期归 Plan 面**——同一条不在两处重复展示（V-16）。
   收尾时必须宣告过期并解除挂起，否则编排协程永远吊在 onPlan 里。

**只做批准/否决，不做"否决 + 反馈 → 重新规划"**：那需要把反馈喂回 planner
并重跑，是另一个设计（且与 §5.2 的判据问题耦合），值得单独一轮。

**真机验证**（deepseek-v4-pro，浏览器点击）：否决 → 零子任务执行、徽章
"计划未获批准"、全页无"异常终止"字样、Plan 面留下"✗ 计划被否决 · 时间 ·
一个子任务都没有发射"；批准 → 签字位退出待办区、状态保持运行中、直播条随即
显示子任务的工具调用。axe 另加一条挂起态扫描——这是**阻塞式**交互，
键盘/读屏用不了它等于整个运行卡死。

**顺带修掉一个先于本功能就存在的缺陷**（真机验证时撞出来的，见第八节）。

### 5.2 需求澄清（`ask_user` 工具）

没有机制让 agent 提问并等待，但 `approval_request` 就是"阻塞等人"的原语，
加一个 `ask_user` 工具即可复用那条通路。

**与哲学高度契合**：`SubTask.acceptance` 已经存在，H1 讲的是"把主观判断降维为
可程序化条款"。需求澄清正是人这一侧的同一件事——**开跑前把验收标准问清楚，
比事后靠 verifier 抢救便宜得多**，这正是 B5 那条「修环境 > 提示词 > 事后核查」
的优先级排序。

**但要先想清判据**：什么时候该问、问几个。容易做过头，值得单独设计一轮。

---

## 六、更早就记下的、本轮明确不做的

- **并行编排的 DAG 甘特图**：R7 做了依赖分层 + 甘特条，够用；自由图布局没必要。
- **run 持久化**：`runs = new Map()` 纯内存，重启即失。
- **日志搜索 / 导出**。
- **v1 报告 §10 的「产物/变更 diff」面板**：需要 diff 数据源，尚不存在。
- **memory 工具在 Web 上的接入**：CLI 有，Web 没有（第六个"harness 有宿主没接"）。
- **监控基线**：运营项。

---

## 七、环境与纪律（接手必读）

- **浏览器窗格**：截图通道在本会话始终不可用（窗格未显示、页面不合成帧）。
  `resize_window` 传显式宽高能拿到真实视口；**强制元素宽度不会触发媒体查询**，
  测窄屏必须用真实视口。窗格偶发整体无响应，`preview_start` 可重开。
- **rAF 陷阱**：隐藏标签页不触发 `requestAnimationFrame`。已在 `core/batch.js`
  处理并有常驻测试，但任何新的帧调度都要记住这条。
- **停后台实例**：Windows 上 `taskkill /PID <pid> /T /F`，停完必须验证端口已释放
  （见记忆 `windows-taskstop-node-zombie`）。
- **改文件用 Write/Edit 工具**，不要用 PowerShell 的 `Set-Content -Encoding utf8`
  （会写 BOM，破坏 tsx 解析 JSON）。
- **Python heredoc 改代码要当心转义**：本会话多次因 `\n` / `\\` 被吞而写出语法
  错误。改代码优先用 Edit 工具。
- **验证纪律**：能行为验证就不要字符串断言。本会话最有价值的几次验证都是
  "让脚本化模型真的去读它收到的东西"——正史有没有带上、图有没有送到，
  都是让对方模型的**回答内容**取决于事实，而不是断言字符串。
- **排查 UI 先确认自己观察的时机**（2026-08-07 踩到）：验文本流式时第一轮
  "失败"其实是我在 run 结束后才点开它——`ensureSubscription` 对终态 run
  按设计不订阅，行为完全正确。先看时序，再怀疑代码。

---

## 八、真机验证撞出来的既有缺陷（2026-08-07）

**编排模式下客户端在 planner 结束时就收摊。**

`ui/public/app.js` 的 `applySegmentDone` 有条快路径：非核查模式下 `done` 即判
整个 run 终止。它不限定来源，而**编排模式下 planner 自己那一轮也发
`done(completed)`**。于是客户端在 planner 一结束就把状态置为 done，控制器随即
`es.close()`——之后的 `plan` / `plan_result` / 子任务进度 / `run_end` 全部收不到，
界面停在"已完成"，还把 planner 的 JSON 当执行者报告展示。

- **触发条件**：`mode=plan` 且未勾"开启独立核查"——**选了"计划编排"之后的默认组合**。
  勾了核查则 `state.verify` 为真、快路径不生效，所以此前的浏览器实测没撞上。
- **它就是 V-01 那条「段终止 ≠ run 终止」**。当时在事件层修过（`done` 只记段，
  run 级收敛由 `run_end` 宣告），**reducer 侧漏掉了同一条**。
- **修法**：快路径限定 `source === "main"`；planner 与 `sN/main` 的 done 不终止 run。
  三条回归锁（planner done / 子任务 done / 单跑 done）。
- **怎么发现的**：做 §5.1 的真机验证时，服务端明明是 `plan_rejected`，界面却显示
  "已完成"。把服务端事件拉下来**在页面里用同一份 reducer 重新 reduce 一遍**，
  得到的状态是对的——于是判定不是 reducer 错，是实时投递断了。这个"重放对比
  live"的手法值得留用：它一步就把"派生逻辑错"和"事件没收到"分开了。

**教训**：一条修在 A 层的纪律，要回头查它在 B 层有没有同构的漏网。
V-01 修的是服务端事件语义，reducer 是另一处同样需要它的地方。

---

## 九、案例 #8 产出的需求（2026-08-07，真实任务驱动）

全案见 [`docs/cases/case-08-crc-selfcheck-hil.md`](cases/case-08-crc-selfcheck-hil.md)。
STM32 真机跨域交付（`stm32-coding` → `stm32-debug`），计划确认门首次用于
真实高风险场景（下一步就写 Flash）。

**已修**：Web 宿主根本没接 MCP——`ui/server.ts` 连 `src/mcp.js` 都没 import，
`AGENT_UI_MCP=1` 只改状态快照文案，于是 `stm32-debug` 这类全 MCP 工具面的包
在 Web 宿主下等于废的。第七个"harness 有、宿主没接"，也是最大的一个。

### 9.1 核查预算不可按领域覆盖 —— ✅ 已实施（2026-08-07）

`src/verifier.ts` 的 `VERIFIER_MAX_TURNS = 15` 是写死常量，`DomainPack` 无法覆盖。
案例 #8 里 `stm32-debug` 的**执行者**有 40 轮护栏，**核查者**只有 15 轮——
而真机核查每条 AC 都要多次探针往返。两轮 verifier 都跑满 15 轮、都从未写出裁决，
最终消息是半截工具调用 → 解析失败 → 重问找不到结论 → fail-closed。

**这是发现 6 的误伤形态②（核查预算耦合）在新领域复现。** 当初的修法是把核查预算
与执行者**解耦**（固定 15）；本案例说明**解耦还不够——15 是按软件域定的数，
硬件域装不下**。

**加重情节**：verifier 不是在空转。它读了 `image_size`/`image_crc32`，然后
`debug_until` 到 CRC 代码附近、`read_memory` 读 `0x08000140`——**它已经嗅到
CRC=0 这个真缺陷并在追查**，是预算把一次正当调查掐断了。预算不足的代价不只是
"没结论"，而是"把已经走对方向的核查掐死在半路"。

**落地**：`DomainPack.verify.maxTurns` → `VerifiedRunOptions.verifyMaxTurns` →
`VerifyOptions.maxTurns` → `runVerifier`；缺省导出为 `DEFAULT_VERIFIER_MAX_TURNS = 15`。
优先级 env（`AGENT_VERIFY_MAX_TURNS`，非法值退出码 1）> 包 > 默认。
`stm32-debug` 设 **30**——依据是案例 #8 那次核查在第 15 轮时已完成 5/6 条验收
并在追查第 6 条，约需一倍余量收口；同时**不高于它自己 40 轮的执行者护栏**
（核查者比执行者还能跑说明护栏的相对关系没想清楚，有一条测试锁住这个不等式）。
编排下**逐子任务按各自的包取**：s1(coding) 与 s2(debug) 的核查工作量差一个量级，
共用一个数就是案例 #8 那个失效。

**行为锁而非字段锁**：让脚本化 verifier 直到第 24 轮才收口 → 缺省 15 轮下落
fail-closed，给到 30 轮拿到实质裁决。写这条时踩了两个坑，都记下来：
① 收口点初设在第 18 轮，被**重问机制**（maxTurns 3）恰好救回——那是重问在按设计
工作，不是预算够用，所以收口点必须 > 15+3；② 脚本模型是全局计数器，**返工轮**
会越过收口点让第二轮核查立刻通过，得关掉返工才测得准。

**顺带**：`/api/harness` 的 `verifierBudgetTurns: 15` 是写死的、UI 还写着"固定"，
这两处在改完之后都变成了谎话——同提交接上，并加了来源标注（默认/包/env）。
接的过程中**第三次踩到字段白名单投影**：`run_config` 的 reducer 分支不列出新字段
就静默丢弃，是那条渲染锁抓到的。加字段必查三处：reduceEvent 投影分支、
派生函数、渲染分支。

**当时未做**：9.2 兜底裁决带过程摘要——它与本项互补（本项让核查跑得完，9.2 让跑不完
时的信号有用），但改的是裁决内容不是预算，单独做。
→ ✅ 9.2 随后已实施（2026-08-08），见第十一节。


### 9.2 fail-closed 兜底裁决要带过程摘要 —— ✅ 已实施（2026-08-08，见第十一节）

`{passed:false, issues:["核查未产出明确结论"]}` 对返工者零信息量——它既可能是
"核查者胡言乱语"，也可能是"做了大量工作没来得及收口"。案例 #8 是后者，而返工者
拿到的信号与前者完全相同，只能从头再来，又烧光 40 轮。
兜底裁决应带上：跑了几轮、调了哪些工具、最后停在哪。

### 9.3 stm32-debug 的 verifier 无任何计算手段 —— ⏸ **降级：暂不做**（2026-08-08 复议）

该包 `builtinTools: ["read_file","write_file"]`、无 `readOnlyCommands`。
凡"设备自报一个派生量（CRC/校验和/哈希）"的验收，verifier 只能确认读到了，
不能独立推导对不对——这类项本该走诚实降级进 `unverified`。
做法二选一：补一条最小只读计算白名单（如 `node <仓库内参考脚本>`），
或在包文档里明确把这类验收归入"必须委托方复核"。

### 9.4 工具名幻觉：MCP 前缀漏写

verifier 调用了 `read_variable`（正确名 `stm32__read_variable`）。
`ToolExecutor` 回"Unknown tool + 可用工具清单"，不致命但每次白烧一轮。
候选：错误消息里对相近工具名做一次最短编辑距离提示。

### 9.5 明确不做

- **芯片系列常量的静态检查**：`1u << 6` vs `1u << 12` 这类错误编译器、静态检查、
  单测全都管不了，真机是唯一裁判。这不是 harness 该解决的问题——**它恰好是
  verifier 的主场**（能力边界附近的语义错误），本案例里 verifier 差一点就抓到了。
  正解是把 9.1 修好让核查跑完，而不是加一层猜芯片型号的检查。

### 9.6 真机域的"只读核查"必须含"把系统带到可观测状态" —— ✅ 已实施（2026-08-08）

用 30 轮预算重跑 s2 时暴露的，比 9.1 更根本（全案见案例 #8 第四点五节）。

verifier 的核查规则第 1 条是「只读核查：不要修改、创建或删除任何东西」。
软件域的只读核查天然完整——文件就在那里，读就是了。**硬件域不是**：
被观测对象必须处于运行态才有意义，而"复位并运行目标"不是持久修改，
却长得像修改。于是纪律良好的 verifier 会避开它。

实测：重跑那次 verifier 68 次工具调用里只用过 1 次 `run_for_duration`，
**一次都没用 `reset_target`**。上一段（executor 的 `stop_debug_session`）
把核心留在非运行态，verifier 连上去 `self_check` 一 halt，读到的是未初始化
SRAM（`magic=0x4E0A43C0`、`PC=0x2000002E` 在 SRAM 内），据此判 AC2/AC3 失败。
委托方 `reset run` + 2 秒后读同一块 RAM：magic=TELM、hclk=16000000、
size=532，PC 在 `main()` 里——**固件一直是好的**。

它并没有胡说：advisory 里已经推断出"可能是板子未处于正常运行态、固件本身
可能是正确的"，**只是没去做那个能证实推断的动作**。

**做法（候选，未定）**：
- `VerifyOptions` 增加"可用的状态复位动作"声明（领域包给出，如
  `reset_target` / `flash_and_run`），并在 verifier 提示里明写
  「只读指的是不改产物，不含不许让被测系统跑起来」；
- 或由 executor 负责"交接时把板子留在运行态"（`stop_debug_session` 前
  先 `reset run`），把责任放在交接契约而不是核查者身上。

**前者更正**：责任在核查者——它必须能独立把系统带到可观测态，否则它对
执行者的交接状态有隐性依赖，那正是 fresh-context 核查要消除的东西。

### 9.7 30 轮也不总是够（同次重跑）—— ✅ 已实施（2026-08-08，收口续跑，见第十一节）

返工后的 round 1 仍跑满 30 轮、仍空输出落 fail-closed。9.1 把"从不收口"
改善成"能收口一次"，没有根治——真正卡住它的是 9.6。**先修 9.6 再谈加预算**，
否则只是让一次注定无效的核查跑得更久。

---

## 十、9.6 的实施与三跑对照（2026-08-08）

**真凶不是泛化纪律，是领域包自己的指令。** `STM32_VERIFY_INSTRUCTIONS` 第 4 条
原文「不要 flash、不要 reset、不要写内存」是一刀切——verifier 不是从泛化的
"只读"推断出不能复位，是**领域包明确禁止了它**。

而这条禁令写得没错，只是**只对一种核查形态成立**：故障现场核查里复位会毁掉
`.noinit` 闩锁 / CFSR / 异常栈帧（案例 #1/#3 的血泪，必须保留）；运行行为核查
恰好相反，不复位就读到上一段会话遗留的未初始化 SRAM。

**所以不是放宽纪律，是把两种形态分开写清楚**（杠杆定律：成文规则冲突用确定性
规则消除，不是靠更强的模型去权衡）：

1. `STM32_VERIFY_INSTRUCTIONS` 显式分叉 (a) 故障现场 / (b) 运行行为，各自给出
   该做什么、为什么，**并给出"怎么认出板子没在跑"的判据**（幻数不对 / PC 不在
   main 相关代码 / 多个字段像随机数）——否则模型只能猜，猜错就把好固件判成假报告。
2. 通用 verifier 提示的只读那条澄清：只读针对【产物】，**不等于"不许让被测系统
   运行"**；具体以领域核查方法为准，**没有领域说明时给出自足兜底**。
   （初版在这里引用了一个不存在的章节，是既有测试当场抓到的。）

**明确不做**：不新增 `DomainPack.verify.stateSetup` 字段。根因在指令文本，加字段
等于把同一件事拆到两处声明。按 P2 按需晋升——等真出现"指令写不下 / 需要程序化
读取"再晋升。

### 三跑 A/B/C（同一份产物，ELF SHA256 三跑逐字节相同）

| | ① 15 轮 | ② 30 轮 | ③ 30 轮 + 分形态 |
|---|---|---|---|
| `reset_target` | — | **0 次** | **2 次** |
| 实质结论 | 无 | ✗ 误判 | **✓ 精确命中真缺陷** |

第三跑独立读到与委托方完全相同的寄存器证据（`0x4002381C = 0x00008000`），
AC5 判失败、AC6 落 `unverified`——正确答案。

**定论**：核查失败要分三层归因，顺序不能颠倒——
①**核查者被成文规则挡住**（本例，改规则）→ ②**预算不够跑完**（9.1，加预算）
→ ③**缺取证手段**（9.3，补白名单）。先怀疑规则，再怀疑预算，最后才怀疑能力。
本例若只加预算（②），得到的是一次跑得更久的错误裁决。

### 9.7 仍开着 —— ✅ 随后已收口（见第十一节）

第三跑 round 0 依旧跑满 30 轮空输出落 fail-closed，靠返工后的 round 1 才拿到
裁决。**首轮收口仍不可靠**——这条与 9.2（兜底裁决带过程摘要）是同一个现场的
两面，建议一起做。

---

## 十一、9.7 + 9.2 的实施与四跑对照（2026-08-08）

**9.7 收口续跑**：撞满预算时最终消息往往是半截工具调用、文本为空——原有的
"重问"另起新 loop 只喂原始文本，文本为空时直接跳过，**整场核查连同已取到的
全部证据一起作废**。正解是续跑同一会话（`runContinuation` 带上正史），
要求"别查了，用已有证据下结论"。收口预算 `VERIFIER_WRAPUP_MAX_TURNS = 2`
刻意很小：只允许写裁决、不允许继续取证，大了等于偷偷放宽调查预算。

收口提示明写「没查完的进 unverified、**不得因没查完判 failed**」——否则预算
用尽会直接退化成 fail-closed 第三种误伤形态（案例 #4 为此烧过 22 轮空转）。

**9.2 兜底带过程摘要**：summary 现在带上为什么没收口、多少次工具调用及分布、
最后一条消息片段，并明写"这不等于产物有问题"。零工具调用单独措辞——
"核查很可能根本没有开展"与"查了很多没收口"是两种完全不同的故障，返工策略不同。
`issues[0]` 哨兵原文不动（`isParseFailure` 与界面核查饥饿判定都靠它）。

### 四跑对照（同一份产物，ELF SHA256 四跑逐字节相同）

| | ① 15 轮 | ② 30 轮 | ③ +分形态 | ④ +收口续跑 |
|---|---|---|---|---|
| `reset_target` | — | 0 | 2 | 2 |
| round 0 | ✗ | ✓ 但误判 | ✗ 空输出 | **✓ 19 轮自行收口** |
| round 1 | ✗ | — | ✓ | **✓ 31 轮 = 30 调查 + 1 收口** |

**直接证据是 round 1**：31 轮那一轮的 `unverified` 原文含「**预算用尽**且缺乏
独立 CRC32 计算手段」——正是收口提示要求的措辞；同一位置在第三跑是空输出落
fail-closed。round 0 这次 19 轮就自行收口、未触发续跑，**因此不能拿它当 9.7
的证据**，只能说明这次收敛得快。

### 9.8（新）执行者宿主级失败同样丢掉全部上下文 —— ✅ 已实施（段级续跑）

第四跑首次尝试：执行者跑到第 29 轮 API 超时，`stopReason=error` →
`runVerified` 按既有设计跳过核查直接返回。而它已经做了 **28 次工具调用**、
最后一句是「让我验证 RCC 寄存器布局是否正确」——**正朝着真缺陷去**。
一次瞬时端点故障把整段工作连同进展全部作废，返工从零开始。

**这与 9.7 同构**：9.7 救的是"核查没来得及收口"，9.8 是"执行没来得及交付"。
`runContinuation` 那条通路同样适用（多轮对话、返工 inherit、9.7 收口都在用它）。
候选做法：主 run 因**瞬时**错误终止时（`isTransientApiError` 已能分类），
带着正史续跑一次而不是整段作废；非瞬时错误（认证/4xx）照旧快速失败。
→ ✅ 已按此实施（2026-08-08）：`orchestrate.ts` 段级续跑——`segment_resume`
事件 + `SEGMENT_RESUME_NUDGE`，仅瞬时错误、带正史续跑，默认 1 次；
人主动中止（`aborted`）不续。

### 9.9（观察项，未判定）verifier 用了 write_memory

第四跑 verifier 用了 5 次 `write_memory`——在写 `RCC_AHBENR` 测试那个位写不写
得进去（`unverified` 里"RCC_AHBENR 写入不生效"就是这么测出来的）。这是很漂亮的
独立取证，但也说明 9.6 那句「只读针对产物」可能比预期放得宽。
**尚未判定是否收紧**：它没有改动交付物，而"写寄存器验证硬件行为"确实是硬件域的
正当核查手段。先记着，等再出现一次不当使用再定。

---

## 十二、清单收尾（2026-08-08）

### 9.4 未知工具名给最近候选 —— ✅ 已实施

实测形态：verifier 调 `read_variable`，正确名是 `stm32__read_variable`——MCP 工具
带 `<server>__` 前缀，模型容易漏。原来只丢一份全量清单，工具面二十几个时模型得
自己在里面找，每次白烧一轮。

`nearestToolNames` 两条判据按可靠性排序：①**后缀命中**（实测最常见形态，几乎不
误报，优先给）；②**编辑距离 ≤ 名长/3**（拼写手滑，阈值卡紧）。八竿子打不着就
不提示——**猜错方向比不猜更糟**。候选只是提示，全量清单照给。

### 9.3 降级：暂不做（复议理由）

原判断是"补一条最小只读计算白名单"。复议后**不做**，两条理由：

1. **三值裁决已经正确处理了它。** 案例 #8 四跑里，AC6 每次都精确落进
   `unverified` 并注明「无可用的宿主侧 CRC32 独立计算手段」，移交委托方复核，
   委托方（用独立参考实现）完成了比对。**这是诚实降级在按设计工作，不是失败。**
   按 P2 按需晋升——没有实测证明它不够用。
2. **给这个包加计算手段的代价被低估了。** `selectPackTools` 给执行者与核查者
   **同一套工具面**，所以给 verifier 加 bash 等于也给执行者加。而
   `stm32-debug` 刻意不给 bash 是 v1.0 演示换来的：给了 bash，执行者会绕开 MCP
   自建 openocd/gdb 调试栈，还会 taskkill "清理"时扫死共享的 MCP server。
   真要做，得先有"核查者专属工具面"这个能力——那是比 9.3 本身大得多的改动。

**若将来要做**：正确形态是 `DomainPack.verify.extraTools`（核查者独有），
而不是往包的公共工具面里加。等出现"三值降级不够用"的实测再动。

### 委托方界面反馈四步（2026-08-08，已全部交付）

对话主干化 → 产物清单 → 装配状态条 → 低切换成本三件套。四步之外还顺手做了
流式匀速放行、代码高亮、贴底跟随与跳转箭头、未读星、`.env` 装载、L6 运行台账。

**这一轮最该记住的三条**（均已进 findings）：

1. **装配条自己说了三处谎**（发现 46 的同族）——包括拿案例 #8 当"换模型可对照"
   的证据，而那次全程单模型、模型是控制量。**听起来对但经不起查**的结论，
   我自己也会写。
2. **停止按钮第一版停不下来**（发现 50）——signal 没传给 SDK，按下 8 秒仍在跑。
   一个不能停的停止按钮比没有更糟。
3. **我被自动化环境骗了两次**（发现 49）——"滚动是空操作"与"主线程卡 7 秒"
   都是观测面板的性质，不是产品的性质。

### 仍然开着

- ✅ **直播条思考流到约 2000 字冻住（2026-08-15，委托方实测报告）——已修。**

  现象：Web UI 里思考流到约 2000 字就停住不动，要过好一会才整块出现。
  根因是**用绝对计数去 slice 一个滑动窗口**：`revealed` 单调增长，而直播缓冲
  `LIVE_TEXT_CAP=2000` 且只留尾部。撞上限那刻三件事连锁——
  ① `arrived` 取自缓冲长度 → 被钉死在 2000；② `revealed` 随之不再变化；
  ③ 节拍器里 `changed` 恒为 false → **再也不重绘**。屏幕冻在那一帧，直到本轮结束、
  turn 级 `assistant_thinking` 走正常 reducer 整块到达——正是"过一会才显示"。

  修法：另记**单调累计到达字数**（不受上限影响），`revealed` 对它计数；
  再由新的纯函数 `revealedWindow` 把绝对计数换算成窗口内偏移。

  **结构性的一点**：这段逻辑原来住在 `ui/public/index.html`，**零测试覆盖**——
  正是本仓库那条「核心可测、壳不可测的分界线就是缺陷分布线」的活标本
  （案例 #10 返工 1 同款）。把换算挪进 `app.js` 做成纯函数是结构性修复而非补丁，
  锁才有地方立：6 条纯函数锁 + 5 条壳侧源码锁（照 B1 抠源码的口径）。

  **顺带修掉同一函数里的第二个缺陷**：节拍器是**全局单例**且闭包捕获 runId——
  A 在流时选到 B，B 的表起不来、A 的表还按 A 跑，B 的直播条一个字不动。改为逐 run 一个。

  变异验证 5 个全部抓红，其中两条第一版逃掉：①「只断言 revealTickers 这个名字存在」
  抓不住 `size > 0` 的退化（名字还在，行为退回单例）；②窗口位置那条我自己的
  算术断言写错了（滞后量相同时位置本就该相同，那不是 bug 是正确性质）。
  真机复核：`npx tsx ui/serve.ts` 起页面、`import('/app.js')` 确认导出与 import 接线
  （测试直接 import app.js，**碰不到 index.html 的 import 列表**——那行错了界面会整个白掉），
  并在浏览器里跑了 20 批 × 250 字的形态复现，全程尾部非空。


- ~~**§2.1 结构化输出**~~ ✅ **已实施（2026-08-15）——但做出来的形状与原案不同，
  因为数据不支持原案。**

  台账攒到 **52 次裁决**时判据开火给出 `do`。可是把那 71.2% 拆开看：
  **wrapup 69.2% / reformat 1.9% / failed 0%**。原案（`response_format: json_schema`）
  治的是 reformat 那一格——**1.9%**。真正的大头 wrapup 是"跑满预算从没写出结论"，
  那不是格式问题，`response_format` 对它一点用没有。判据自己写着"做，且**优先针对
  占比大的那一段**"，所以照数据改了形状：

  **把交付做成工具**（`submit_verdict` / `submit_plan` / `submit_shards`）+
  `AgentConfig.terminalTool`（调用即以 `completed` 收尾）+ 收口段
  `tool_choice: {type:"tool", name}` 强制交付。为什么这才对症：在自由文本契约下，
  "停止取证、开始下结论"这个转换**没有可执行的着力点**，模型永远可以再查一件事；
  给它一个工具，转换才第一次变成一个动作，harness 也才有地方强制它做（P6）。
  这同时把 B0b 那个 `toolChoice:"none"` 收紧了一级——禁工具说得出"别查了"，
  说不出"现在就产出这个形状"。

  降级臂完整保留：文本 JSON 契约仍在提示里、`parseVerdict` 仍在兜底、
  工具入参不合法时降级回文本解析。

  **真机探针实测（2026-08-15，`eval/probe-toolchoice.mjs` + 手工矩阵）——
  当场抓出这个实现会在本机 400：**

  | 请求 | 结果 |
  |---|---|
  | `deepseek-v4-pro` + 强制工具（默认思考开） | **400**「Thinking mode does not support this tool_choice」|
  | 同上 + `thinking:{type:"disabled"}` | **200，真的返回 tool_use** |
  | `claude-opus-4-8`（本机实际在用的名字）+ 强制工具 | 400（同上，端点把它路由到 `deepseek-v4-pro`）|
  | 同上 + `thinking:disabled` | 200 SUPPORTED |
  | `deepseek-chat`（无思考）+ 强制工具 | 200 SUPPORTED（实际由 `deepseek-v4-flash` 服务）|
  | OpenAI wire `tool_choice:{type:"function"}` | 200 SUPPORTED（无思考字段，天然没这问题）|

  **这不是端点怪癖，是 Anthropic 协议本身的约束**（扩展思考开启时 tool_choice
  不允许点名具体工具），所以修在 wire 层且两种模式统一：`buildParams` 一旦带
  强制工具就显式发 `thinking:{type:"disabled"}`。语义上也正好对——需要强制交付
  的只有收口段，那一段本来就不该再思考。
  另加**降级臂**：客户端捕获这类 400 后剥掉 `tool_choice` 重发一次并记住结论。
  必须在 L0 做——400 是永久性错误，loop 的分类会直接 `finish("error")`，
  于是"强制交付"这个增强反而会杀死收口救援，比不做还糟。

  **端到端真机验收通过**（`eval/live-terminal-tool.ts`）：真 verifier 核查一个
  植入了不符的文件 → `recovery=tool`、`passed=false`、自己数出 4 行 ≠ 报告的 5 行
  （还识破了末尾换行造成的假第 5 行），5 轮、缓存命中 83.6%。

  **顺带解开一个悬案**：`.env` 里 `AGENT_MODEL` 注释着 → 落到默认值
  `claude-opus-4-8` → 被 DeepSeek 端点接受并路由到 `deepseek-v4-pro`。
  台账里那 59 条 `model: null` 记录，服务方就是它。建议把 `AGENT_MODEL` 显式
  写进 `.env`，否则所有跨 run 对照都缺一个已知变量。

  **判据换岗**：`STRUCTURED_OUTPUT_RULE` 打完自己那一仗后**有记录退役**
  （留在 `src/ledger.ts` 里能原样复现那一刻的读数，有测试钉着），接手的是
  `STRUCTURED_OUTPUT_EFFECT_RULE`——问"生效了吗"，阈值同样先写后收。
  配套一条仪器纪律：新增 `structuredDelivery` 构建标记，效果读数**只算实施后的行**，
  否则那 52 条基线会把 tool 占比永久稀释成"端点不认"。**被旧数据污染的读数
  等于没有读数**——与当年 FakeModelClient 灌进 86 条假运行同族。

  宿主同提交接上（第零节那条规律）：`ui/public/app.js` 的健康路径集合从
  "只有 direct"扩成 `{tool, direct}`——不改的话，一次完全正常的结构化交付会被
  界面画成"核查者收口不稳"（V-04）。加了源码级三处口径一致锁，照 B1 那条办法。

  **验证**：857→876 单测全绿、`tsc` 干净、**12 个变异全部被抓红**
  （终结工具失效 / 交付后不收尾 / 正史缺回执 / 收口退回禁工具 / 入参校验放松 /
  效果读数不隔离基线 / OpenAI 映射写错 …）。**真机未跑**——tool 占比现在是 0，
  等真实任务攒够 20 次裁决后 `npm run ledger` 会自己给结论。

- ✅ **§5.2 需求澄清（ask_user）已实施（2026-08-15）**。
  卡了很久的是判据不是实现，所以照 §5.1 的办法**先写四个设计决定**：
  ① **默认关**（CLI `--ask`）——宿主也被脚本化驱动，默认开会让无人值守挂死（V-01）；
  ② **配额由 harness 强制**（`maxQuestions` 默认 3，`AGENT_MAX_QUESTIONS` 可调）
  ——"问几个"正是这条挂了这么久的真正原因，而答案不能是提示里的"别问太多"（P6）；
  配额用尽**不把工具从面上摘掉**（中途改工具列表 = 缓存前缀全灭，P3），
  而是返回可操作的 `is_error` 让模型带着假设继续；
  ③ **只有执行者能问**——`withoutAskUser` 在 `runVerifier`/`runPlanner` 里硬剔除。
  核查者一旦能问委托方要答案，"独立核查"的全部公信力当场作废；这条**必须在
  harness 层**执行，因为两个角色是 `{...cfg}` 继承宿主那份工具面的；
  ④ **未应答 = 过期不是失败**——回一条"按你的最佳判断继续，并把假设写进报告"，
  中止时也走这条（停止按钮不该变成一次提问，也不该被画成 error）。
  ⑥ **一次提交一组问题（1~4 个），配额计【打断次数】不是【问题数】**
  （委托方实测场景催生，2026-08-15）：「给这个项目开发一版 Desktop UI」一开口
  就是三个**正交**未知——技术选型 / UI 风格 / 这次做到什么程度。按"一次一个
  问题"的旧设计，第一轮澄清就把额度用光，还要三次往返。
  **贵的是打断人，不是问题本身**：三题一屏答完是一次打断，拆三轮就是三次。
  单位错了，配额就一定是错的。
  ⑦ **鼓励开工前问**（时机写进工具描述）：范围与选型类问题做到一半才问，
  代价已经付出去了——"修环境 > 提示词 > 事后核查"（发现 2.5）的人机版。
  ⑤ **选项必填 2~4 个**（委托方追问"是不是跟主流 agent 一样给选择题"时定的）：
  原先 `options` 是可选的，模型不给就退化成开放式提问——而"建议模型给选项"
  写在描述里就只是提示，同一轮里配额已经按 P6 做成硬的了，选项没理由例外。
  schema 声明 minItems 之外 `execute` 自己再查一遍（本轮真机探针刚证明
  「schema 声明了」≠「端点执行了」）。**自由输入不取消**——宿主始终允许绕过
  选项直接写，相当于主流 agent 的「其他」逃生口；强制的是模型先想清有哪几条路。
  入参纪律：`fallback` 必填——先想清默认路线再来占用委托方的注意力。

  **CLI 与 Web 宿主同提交都接上了**（第零节那条规律，不留缺口）。Web 侧照计划
  确认门做了挂起/应答/过期三条 durable 合成事件 + `POST /api/runs/:id/answer`
  + ActionRail 提问卡（选项按钮 / 自由输入 / 「让它自己定」三个出口）+ axe 扫描。

  **接宿主时被测试当场抓出一个真缺陷**：停止按钮按下去 run 不会停——
  abort 端点只解除了挂起的审批与计划门，忘了提问，执行协程仍吊在 `execute` 里
  （V-01 原样重演）。两处都修了：宿主侧把"解除挂起且不带答案"收敛成
  `expireQuestion` 单一出口（此前停止那条只 settle 不发事件，挂起态消失却没记录）；
  **工具侧与中止赛跑**——不变量不能靠每个宿主都记得（P6），signal 一响就自己
  走未应答出口，任何宿主都造不出这个挂死。
  另有一条变异（提问不进 `needsAttention`）暴露出：卡片自身会显示，但外层的坞
  仍盖着，整块「需你决定」一个像素都看不见——断言必须连坞一起。

  **注意"挂死"指的是没有人的场景**（无人值守 / 已按停止 / 宿主关停），
  **不是**"有人在时不该等"。没有任何超时：run 活着、人在，提问卡就一直等。

  **触发条件扩了一类（实测驱动）**：委托方在 Web 上问「今天天气怎么样」，
  回放那条 run（`c29064eb`）发现——模型其实**问了**，只是用嘴问的
  （「我不知道你所在的城市…可以告诉我吗」），然后收笔结束，得再发一轮对话才能答。
  这正是 ask_user 要解决的形态。但按第一版描述的字面读它**不该问**：我只写了
  "判据歧义"，而"你在哪个城市"是**缺一个事实**——letter-vs-spirit 在我自己写的
  工具描述上重演了一次。现在两类都写清：① 判据歧义；② **只有委托方知道、
  用尽工具也查不到的事实**（所在城市/用哪个账号/「我们的项目」指哪个仓库）。

  32 条工具测试按各条决定分组 + 9 条宿主端到端测试，**变异全部被抓红**
  （含两条第一版逃掉的：配额单位、前端只渲染第一题）。其中「配额按轮次而非
  按题数扣」需要专门构造才分得出——额度检查在自增之前，两种实现在第一次调用上
  表现相同，第一版测试因此漏判。

- ~~**§2.1 结构化输出**（`tool_choice` / `response_format`）—— **仪器已就位，等样本（2026-08-08）**。~~
  （下面保留当时的原始记录：判据是怎么定下来的，比结论本身更值得留。）

  此前这条写着"建议先埋计数器量一量"。查下来发现**等不到**：`recovery` 只活在
  事件流与 `ui/server.ts` 的内存 Map 里，**进程一重启样本归零**；CLI 那条路径
  连运行记录都不写。跑一百次和跑一次，可数的样本都是 0。
  这是项目自己那条 P6（不变量靠 harness 不靠自觉）没有用在**研究本身**上：
  **一条要靠"下次注意看"才能获得的证据，等于没有证据。**

  已落地 **L6 运行台账**（`src/ledger.ts`）：两个宿主收尾各追加一行 JSONL，
  只记元数据（不记任务原文、不记会话正文、不记工具入参），
  含逐轮 `recovery`、按角色的工具直方图、核查是否撞预算。`npm run ledger` 读数。

  **判据在收到任何数据之前就写死在代码里**（`STRUCTURED_OUTPUT_RULE` +
  `decideStructuredOutput`，每个分支都有测试钉着）：

  | 观察到 | 结论 |
  |---|---|
  | 出现 ≥1 次 `failed`（fail-closed 兜底裁决） | **立刻做**——那是误伤，压过样本量门槛 |
  | 裁决样本 < 20 | 不下结论，继续攒 |
  | 非 `direct` 占比 < 5% | **关掉 §2.1**，把这个数字写进 backlog |
  | `reformat + wrapup` > 20% | **做**，且优先针对占比大的那一段 |
  | 落在 5%~20% 之间 | 说清是灰带，再攒一倍样本 |

  > 阈值先写、数据后收，是它算证据而不是事后合理化的分界线。
  > 收完数据再定阈值，等于用数据反推一个想要的结论。

  **上线当天踩的坑**：忘了给台账设仪器纪律，一跑测试套就灌进 **86 条假运行、
  22 次裁决全是 `direct`**——FakeModelClient 的裁决永远可解析，那个 100% 正好
  会把判据推向"关掉"。**用假模型的数去判模型行为，是最坏的一种假证据。**
  现在注入了 `modelClient` 就默认不记账，并有回归锁。
- ~~**思考流式**（`thinking_delta`）~~ **已接通（2026-08-08 确认全链路在场）**：
  两个客户端都发 `kind:"thinking"` 增量（`model-client.ts:47` / `model-client-openai.ts:78`）
  → `loop.ts:212` 转成 `thinking_delta` → `ui/server.ts:629` 走 `event: delta` 命名通道带
  `kind` → 前端 `deltaBatcher` 分两路进 `liveThinking` → 直播条在正文之前显示。
  `src/types.ts` 里那句"`thinking_delta` 尚未接"是遗留注释，已改。
- **运行历史不落盘：宿主一重启，全部运行连同会话正文一起没**（2026-08-08 记）。

  `ui/server.ts` 的 `runs` 是个内存 `Map`（认符号别认行号——行号每改一次代码
  就漂一次，这份文件已经为它更正过两回）。实测后果不只是"看不到旧记录"：
  委托方那台上开着四条对话，**想升级到新版界面就必须重启，重启就全丢**——
  于是"用新功能"和"留住已有工作"变成二选一。这是产品级的选择题，不是技术债务。

  同一个根因还产生过另外两处（都已单独处理，但根子在这里）：
  §2.1 的 `recovery` 样本随进程一起归零（催生了 L6 运行台账）；
  续跑要靠内存里的 `run.loop` 与 `run.history`，所以重启后**所有运行都不能再追加**。

  **不急着做，因为几个问题得先想清楚**（想歪了比不做更糟）：
  ① **存什么**——事件流可以（每条几百字节），会话正文不行（可达数 MB/段），
     那正是当初把 transcript 从 SSE 里摘出来的理由；
  ② **存哪**——单文件 JSONL 还是每 run 一个目录？后者才好做"删掉某一条"；
  ③ **存多久**——没有清理策略的持久化最后都会变成一块没人敢删的硬盘占用；
  ④ **能不能恢复续跑**——`run.loop` 是活对象，落盘再复原等于要重建 AgentLoop
     与 ContextManager 的水位记忆，这一步的代价可能比前三步加起来还大。
     **不能恢复也要说清楚**，否则界面上那个「继续对话」按钮在重启后就是骗人的。

  > 台账那一轮的教训在这里同样适用：**先定判据再动手**。
  > 不然很容易做成"什么都存"，然后发现最想要的那件事（续跑）恰恰没解决。

  → ✅ **已收口（2026-08-09）**：四问答案与实施见交接页 B2 条目
  （`ui/history.ts`；续跑走的是"明确不能且照实说"，不是"什么都存"）。

- **`aborted` 是本轮新增的第七个终止值**（停止按钮催生）。它加剧了下面那条口径不一致——
  收口时按 `classifyStopReason` 为准回填，别再各写各的。

- **终止原因的口径三处不一致**（2026-08-08，装配条文案交叉核对时发现）。
  `docs/03-interfaces.md` 列五值、`src/types.ts` 列六值、而 `classifyStopReason`
  实判**八个**具名值（多了 `budget_exhausted` / `refusal` / `plan_rejected` /
  `plan_gate_expired`，后两个是计划确认门上线时加的）。
  三处都在被人引用，谁引到哪一处就得到哪个答案。
  **界面文案已先行避险**：装配条的护栏说明不写具体数目，并有回归锁挡住
  （`ui-patch.test.ts`「护栏说明不写死终止值的个数」）。
  收口这件事本身要一次做全：以 `classifyStopReason` 为准回填 types 与 docs，
  并加一条"三处枚举必须逐值一致"的锁——否则下次加一个值又会漂开。
  → ✅ **已收口（2026-08-09）**：做法与顺带修出的 expired→plan_rejected
  真缺陷见交接页 B1 条目。

- **9.9** `write_memory` 观察项 —— **改为自动检出（2026-08-08）**。
  此前那次是读 verifier 日志时**碰巧看见的**，仓库里没有任何"哪个角色调了哪个
  工具"的记录，要再看见一次得靠同样的运气。现在台账按角色记工具直方图，
  `npm run ledger` 会把 verifier 侧的写类调用单独列出来，出现即入册。
  **注意**：这个现象只有 **CLI + 领域包**那条路能产生——Web 宿主根本没接
  `MemoryStore`（`ui/server.ts` 零命中）。所以要它出证据，得跑 CLI 带 pack 的真机任务。
- ~~**AC2-18** v1 九条回归复验、**§1.5** 状态表重排~~ **均已做（2026-08-08）**，见第一节。
  剩余人工缺口**一条**：**NVDA 听感**（AC-06 的非结构部分）——它需要一个真人戴耳机听，
  自动化替不了。（AC2-11 的单帧毫秒数当天已量完，见第一节第 3 条。）
- ~~**case-07** 更新至 v2 全案~~ **已定案（2026-08-08）**：**不重写正文**，
  改为补两处——① 文末「后续」表列全 v2 之后的六条去向（且刻意不写单测总数，
  见 findings 47）；② 新增第六节「回望：这个案子最终讲的是什么」，写它作为
  **唯一被反复重做的案子**才照得出的那条：*一条验收被"关闭"之后会在无人察觉时
  失效，因为承载它的结构被重做了，而看守它的断言还在绿着*。
  正文里 v1 时期的结论（「全部关闭」「225 单测」）就地标注为**那一刻的快照**。

  > 为什么不重写：正文记的是 v1 那一轮的第一手经过，重写等于把它抹掉换成一份
  > 二手概述；而 v2 的经过已在 `remediation-v2-status.md` 逐条带证据记着，
  > 抄进案例文档只会造成两份会漂移的记录。**案例文档的价值是"这件事教了什么"，
  > 不是流水账**——所以补的是回望，不是续写。
- ~~**滚动锚定未验证**~~ **已作废（2026-08-08）**：那个补偿函数连同它的五条测试
  已经删掉了。委托方给的是结构解法——输入框移到底部、待办钉在其上方且**在滚动容器
  之外**，于是它变高变矮根本不平移内容，补偿无从谈起。见 findings 42。
