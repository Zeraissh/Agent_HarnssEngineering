# 非敏感默认项：提交到 git，与本地 .env 中非密钥行对齐时可合并进云端 .env。
# 密钥类变量请只在 Cursor Environment Secrets 配置（与本地 .env 同名）。
#
# DeepSeek Anthropic 兼容端点（本地常用配置；密钥仍走 ANTHROPIC_API_KEY Secret）
ANTHROPIC_BASE_URL=https://api.deepseek.com/anthropic
AGENT_MODEL=deepseek-chat
