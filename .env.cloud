# 非敏感默认项：提交到 git，新 Agent 启动时由 scripts/cloud-sync-env.sh 写进工作区 .env。
# 同名 Secret 覆盖这里的值。密钥类变量只配 Cursor Environment Secrets，绝不写进本文件。
#
# 这里逐字对齐本机 .env 的非敏感部分（deepseek anthropic 兼容端点），
# 于是云端只剩 ANTHROPIC_API_KEY 一项需要人工填 Secret。
ANTHROPIC_BASE_URL=https://api.deepseek.com/anthropic
OPENAI_BASE_URL=https://api.deepseek.com

# describe_image 工具：配了才上工具面；端点与 key 沿用上面的 ANTHROPIC_*。
AGENT_VISION_MODEL=deepseek-v4-flash-vision-exp

# 有意不钉 AGENT_MODEL：本机 .env 也没钉，走代码默认 claude-opus-4-8，云端与本机同档。
# 钉住会让云端静默换模型——此前钉的 deepseek-chat 实测同一句话只出 1 个输出 token
# （claude-opus-4-8 出 93），是另一档能力，不该由环境配置替使用者决定。
