---
description: '单测全绿而真机必错的五类缺陷（rAF/隐藏标签页、[hidden] 被 display 压过、路径分隔符、路径未归一、纯函数与控制器的缝）；测窄屏必须用真实视口；能行为验证就别用字符串断言' (Claude Code memory)
alwaysApply: true
---

<!-- Synced from Claude Code memory on 2026-09-01 -->

---
name: browser-only-defects
description: "单测全绿而真机必错的五类缺陷（rAF/隐藏标签页、[hidden] 被 display 压过、路径分隔符、路径未归一、纯函数与控制器的缝）；测窄屏必须用真实视口；能行为验证就别用字符串断言"
metadata: 
  node_type: memory
  type: feedback
  originSessionId: 8b274376-2e13-4846-ae93-526a02432ed9
  modified: 2026-08-07T03:46:33.011Z
---

Agent_Design Web UI v2 系列里，**单测 400+ 全绿但真机一跑就错**的缺陷有四个，四个都是同一类：**测试环境缺少真实运行时的某个维度**。

1. **`requestAnimationFrame` 在隐藏标签页不触发** → 用 rAF 做事件批处理时，后台标签页的事件在队列里无限堆积、界面永不更新（等审批的人永远等不到卡片）。jsdom 里 `visibilityState` 恒为 `visible`，测不出。修法：隐藏时退回定时器 + `visibilitychange` 立即补齐。
2. **`[hidden]` 被作者 `display:` 压过** → UA 样式表的 `[hidden]{display:none}` 优先级极低，`.live-strip{display:flex}` 直接盖掉它，于是已终止的运行仍显示"等待模型响应"。jsdom 不做样式级联，只能验属性设没设、验不了生没生效。修法：全局 `[hidden]{display:none!important}`。
3. **路径分隔符正则写漏反斜杠**（`/[\/]/` 而非 `/[\\/]/`）→ Windows 路径切不开。测试数据里若用 `"D:\work\a"`（TS 中 `\w` 是无效转义会被吃掉）则恰好也测不出。
4. **同一路径在不同处形态不一** → `workdir` 没在源头 `resolve`，`D:/a/b` 与白名单里的 `D:\a\b` 字符串不等，**默认工作目录过不了自己的白名单**。单测因 `mkdtemp` 本就返回规范化路径而完全没碰到。

**第五类（2026-08-07 新增，成因不同）——单测覆盖的是纯函数，真机跑的是"纯函数 + 控制器"的组合**：
`app.js` 的 `applySegmentDone` 快路径"非核查模式 done 即终止"不限定来源，而编排模式下 **planner 自己那一轮也发 `done(completed)`**。reducer 单测逐条喂事件、从不关连接，所以永远绿；真机上控制器看到 `status === "done"` 就 `es.close()`，之后的 `plan_result` / 子任务进度 / `run_end` 全丢，界面停在"已完成"。触发条件 `mode=plan` 且未勾核查——正是选了"计划编排"之后的默认组合。

**诊断手法（值得留用）**：界面与服务端不一致时，把服务端事件拉下来**在页面里用同一份 reducer 重新 reduce 一遍**。结果对 → 派生逻辑没错，是实时投递断了；结果也错 → reducer 的问题。一步就把两类原因分开。

**Why**：前四类的共同点是"测试替身把某个维度整个抹平了"——可见性、样式级联、平台分隔符、路径规范化。第五类不同：纯函数本身没错，错在**它与控制器的交互**，而单测按设计不覆盖那条缝。

**How to apply**：
- **纯函数全绿 ≠ 那条路径对**。凡是"reducer 的某个输出会驱动控制器做副作用"（关连接、停轮询、清缓冲），必须另有一条走真实控制器的验证。
- **一条修在 A 层的纪律，回头查 B 层有没有同构漏网**：V-01 的「段终止 ≠ run 终止」在服务端事件层修过，reducer 侧漏了同一条，隔了一整轮才被真机撞出来。
- **测窄屏必须用真实视口**（`resize_window` 传显式宽高）。强制元素宽度**不会触发媒体查询**——我为此误判过一次"窄屏没修好"，其实是测量方法错了。
- **能行为验证就别用字符串断言**。本轮最有价值的几次验证都是让脚本化模型**真的去读它收到的东西**，使其回答内容取决于事实：续跑正史带没带上（模型复述暗号）、图有没有送到 compat 端点（视觉客户端靠有没有收到图决定回答）、工作目录圈禁根有没有真移动（放行 bash 看 `pwd` 实际返回）。
- 涉及路径、可见性、样式生效的改动，**默认假设单测测不到**，安排一次真机复核。

相关：[[agent-design-harness-project]]、[[host-lags-harness]]、[[windows-taskstop-node-zombie]]
