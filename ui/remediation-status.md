# 整改报告 V1.0 · 关闭状态（2026-08-06）

对照委托方整改报告 [`remediation-v1.md`](remediation-v1.md) 的问题编号与验收标准，
逐条记录关闭状态与证据。实施分两轮：s3a（P0 + 文案，commit 4156a57）、
s3b（P1 + P2，commit b1f7dfa）。

## 问题项（第 3 节 R-01 ~ R-08）

| 编号 | 等级 | 状态 | 证据 |
|---|---|---|---|
| R-01 完成运行仍可操作审批 | P0 | ✅ 关闭 | 浏览器实测：运行"已完成"后可操作审批按钮数 = 0；两条审批转只读记录（工具/参数/决策/时间戳）。服务端幂等：重复应答与运行结束后应答均 409，respond 只调一次 |
| R-02 390px 固定侧栏挤压 | P0 | ✅ 关闭 | 390/768/1280/1440 四视口 `scrollWidth == clientWidth`（零横向溢出）；390px 侧栏 `display:none`、布局 column、"← 返回列表"在位 |
| R-03 结果位于长日志之后 | P1 | ✅ 关闭 | 详情页重构为 概览/运行日志/核查 三标签；概览首屏含状态徽章 → 结果摘要 → 核查结论（三值）→ 待复核事项 → 用量 |
| R-04 日志全量平铺 | P1 | ✅ 关闭 | 条目为"动作+对象+结果+耗时"摘要卡；实测成功条目 `log-entry--collapsed`（▸），审批条目 `log-entry--approval`（▾ 自动展开） |
| R-05 键盘/语义缺失 | P1 | ⚠️ 机制到位，专项待测 | 列表项 tabindex + role + aria-selected、输入框关联 label、aria-live 状态区、:focus-visible 均已落地并有静态断言；**完整键盘走查与屏幕阅读器测试未执行**（报告 AC-05/AC-06） |
| R-06 提交按钮对比度 2.53:1 | P1 | ✅ 关闭 | 底色改 `#0969DA`，浏览器实算 **5.19:1**（正文 12.26:1）；新增 vitest 测试从 styles.css 解析色对、按 WCAG 相对亮度公式实算并断言阈值 → 对比度成为回归门禁 |
| R-07 核查区视觉权重过高 | P2 | ✅ 关闭 | 洋红收敛为身份标识（标题/图标/边框/徽章），不再大面积铺底 |
| R-08 列表缺时间/耗时/结果 | P2 | ✅ 关闭 | 列表项显示状态/开始时间/耗时/核查结论，并支持按状态筛选（全部/运行中/已完成/未通过） |
| P2 设计令牌未统一 | P2 | ✅ 关闭 | 颜色/字号/间距/圆角收敛进 `:root`；`:root` 外裸十六进制色值 = 0（静态断言守护） |

## 验收标准（第 13 节 AC-01 ~ AC-10）

| 编号 | 状态 | 说明 |
|---|---|---|
| AC-01 状态一致、无可操作历史审批 | ✅ | 实测 + reducer 测试（done/error 时 pending 转 expired） |
| AC-02 审批首屏信息完整、决策幂等 | ✅ | 幂等 409 双场景测试；审批卡含动作/对象/参数 |
| AC-03 无需展开日志即可判断结果 | ✅ | 概览首屏实测 |
| AC-04 成功折叠、异常展开、两次操作可达原始详情 | ✅ | 实测折叠态 + reducer 测试 |
| AC-05 全键盘可操作、焦点可见 | ⚠️ 待专项 | 语义与样式已落地，键盘走查未执行 |
| AC-06 屏幕阅读器可识别 | ⚠️ 待专项 | 需 NVDA 等工具人工验证 |
| AC-07 对比度 4.5:1 / 3:1 | ✅ | 实算 5.19:1 / 12.26:1 + 回归测试 |
| AC-08 四视口无水平滚动 | ✅ | 390/768/1280/1440 实测 |
| AC-09 200% 缩放可用 | ✅ | 等效 640×360 CSS 像素实测：零横向溢出，三标签与主操作齐全，内容重排正常 |
| AC-10 六类流程端到端 | ⚠️ 部分 | 已实测：正常完成、等待审批、历史查看、核查通过带待复核项；**未实测：审批拒绝、执行失败、核查未通过** |

## 发布门槛（第 14 节）

- P0 全部关闭 ✅（状态冲突、历史审批可操作、窄屏不可用三项均为 0）
- 桌面端核心流程通过 ✅；移动端（390px）核心流程通过 ✅
- 自动化无障碍扫描 ⚠️ 未执行（无 axe 类工具，未新增依赖）
- 审批重复提交率 0 ✅（服务端幂等 409）
- 监控基线 ⚠️ 未建立（属运营项，非本轮范围）

## 剩余项建议

1. **无障碍专项**（AC-05/AC-06）：需人工键盘走查 + 屏幕阅读器；如允许新增 devDependency，可引入 axe-core 做自动扫描并纳入 vitest。
2. **异常流程回归**（AC-10 剩余三类）：审批拒绝、执行失败、核查未通过——可用脚本化 ModelClient 在契约测试层补齐，无需真实模型。
3. 监控基线（状态冲突率、审批处理耗时、结果定位耗时）属上线后运营项。

单测总数：195（s3 两轮 +33）。
