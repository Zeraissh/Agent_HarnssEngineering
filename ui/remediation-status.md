# 整改报告 V1.0 · 关闭状态（2026-08-06）

> **v2 声明**：本表全部已关闭条目（R-01~R-08、P2 令牌统一、AC-01~AC-10）在
> [UI v2 重做](remediation-v2.md)中**持续有效**——回退任意一条即 v2 不通过（AC2-18）。
> v2 的新增条目见 [`remediation-v2-status.md`](remediation-v2-status.md)。
>
> **读法警告（2026-08-08 AC2-18 复验后补）**：下表的**证据**描述的是 v1 那一代
> 界面。v2 之后有几处承载形态已经不存在了（三标签 → 四决定因素卡 + 下钻；
> 提交栏从顶栏移到底部；追加框并进底栏；审批卡搬进 `#action-dock`）。
> 带 ⧗ 标记的行即属此类：**判据仍然成立，但那句话里的具体元素已经不在页面上**。
>
> 有意不删这些证据——它们是当时确实验过的第一手记录，删掉等于抹掉事实。
> 但也不能让人照着去找一个不存在的东西，所以就地标注。
> **复验的当前结论一律以 [`remediation-v2-status.md`](remediation-v2-status.md)
> 的 AC2-18 行为准。**

对照委托方整改报告 [`remediation-v1.md`](remediation-v1.md) 的问题编号与验收标准，
逐条记录关闭状态与证据。实施分两轮：s3a（P0 + 文案，commit 4156a57）、
s3b（P1 + P2，commit b1f7dfa）。

## 问题项（第 3 节 R-01 ~ R-08）

| 编号 | 等级 | 状态 | 证据 |
|---|---|---|---|
| R-01 完成运行仍可操作审批 | P0 | ✅ 关闭 ⧗ | **⧗ 字面判据已被 V-01 推翻**：「运行"已完成"后可操作审批按钮数 = 0」在 v2 里要限定为 **run 级终止之后**——段级 `done` 不再终止 run，返工轮的审批必须仍可点。当前判据与 DOM 级回归锁见 AC2-18 行。原始记录：浏览器实测：运行"已完成"后可操作审批按钮数 = 0；两条审批转只读记录（工具/参数/决策/时间戳）。服务端幂等：重复应答与运行结束后应答均 409，respond 只调一次 |
| R-02 390px 固定侧栏挤压 | P0 | ✅ 关闭 | 390/768/1280/1440 四视口 `scrollWidth == clientWidth`（零横向溢出）；390px 侧栏 `display:none`、布局 column、"← 返回列表"在位 |
| R-03 结果位于长日志之后 | P1 | ✅ 关闭 ⧗ | **⧗ 三标签在 v2 已删**，由「L2 常驻区（页头 → 待办坞 → 直播条 → **结果卡** → 四决定因素）+ L3 下钻面」取代；结果卡排在下钻面之前这一点现已有 `compareDocumentPosition` 回归锁（此前无人看守）。原始记录：详情页重构为 概览/运行日志/核查 三标签；概览首屏含状态徽章 → 结果摘要 → 核查结论（三值）→ 待复核事项 → 用量 |
| R-04 日志全量平铺 | P1 | ✅ 关闭 | 条目为"动作+对象+结果+耗时"摘要卡；实测成功条目 `log-entry--collapsed`（▸），审批条目 `log-entry--approval`（▾ 自动展开） |
| R-05 键盘/语义缺失 | P1 | ✅ | 列表项 tabindex + role + aria-selected、输入框关联 label、aria-live 状态区、:focus-visible 均已落地；**浏览器实测键盘走查已执行**，并据此修复两处 ARIA 结构缺陷（见下） |
| R-06 提交按钮对比度 2.53:1 | P1 | ✅ 关闭 | 底色改 `#0969DA`，浏览器实算 **5.19:1**（正文 12.26:1）；新增 vitest 测试从 styles.css 解析色对、按 WCAG 相对亮度公式实算并断言阈值 → 对比度成为回归门禁 |
| R-07 核查区视觉权重过高 | P2 | ✅ 关闭 | 洋红收敛为身份标识（标题/图标/边框/徽章），不再大面积铺底 |
| R-08 列表缺时间/耗时/结果 | P2 | ✅ 关闭 | 列表项显示状态/开始时间/耗时/核查结论，并支持按状态筛选（全部/运行中/已完成/未通过） |
| P2 设计令牌未统一 | P2 | ✅ 关闭 | 颜色/字号/间距/圆角收敛进 `:root`；`:root` 外裸十六进制色值 = 0（静态断言守护） |

## 验收标准（第 13 节 AC-01 ~ AC-10）

| 编号 | 状态 | 说明 |
|---|---|---|
| AC-01 状态一致、无可操作历史审批 | ✅ | 实测 + reducer 测试（done/error 时 pending 转 expired） |
| AC-02 审批首屏信息完整、决策幂等 | ✅ | 幂等 409 双场景测试；审批卡含动作/对象/参数 |
| AC-03 无需展开日志即可判断结果 | ✅ | 概览首屏实测 |
| AC-04 成功折叠、异常展开、两次操作可达原始详情 | ✅ | 实测折叠态 + reducer 测试 |
| AC-05 全键盘可操作、焦点可见 | ✅ ⧗（2026-08-08 复验发现两处回退，已修） | **⧗ 元素清单与颜色都已过期**：提交栏移到底部后 Tab 序整体重排（实测 11 个元素，不是下面写的 10 个），焦点色也不再是 `#0969da`（改用 `--focus-ring` 令牌）。复验抓到两处**真的看不见焦点**：`.submit-bar textarea:focus` 压掉了全局 `:focus-visible` 的环；`#file-upload` 是 `.sr-only` 但可聚焦、环被 clip 裁没。均已修 + 加静态门禁。原始记录：浏览器实测：Tab 序完整无陷阱（筛选→运行项→任务输入→核查开关→运行任务→允许/拒绝→理由→标签页，10 个交互元素全可达）；`:focus-visible` 实测渲染 `solid 2px #0969da`；标签组按 APG 模式支持 ←/→/↑/↓/Home/End，切换后焦点跟随 |
| AC-06 屏幕阅读器可识别 | ✅ 结构层 / ⚠️ 听感待人工 | 实测修复两处结构缺陷（孤儿 `role=option`、tab 三件套不全，见下）；aria-live 实测播报"新任务已提交：…"。**结构语义已可被辅助技术正确解析；真实 NVDA 听感体验仍建议人工过一遍** |
| AC-07 对比度 4.5:1 / 3:1 | ✅ | 实算 5.19:1 / 12.26:1 + 回归测试 |
| AC-08 四视口无水平滚动 | ✅ | 390/768/1280/1440 实测 |
| AC-09 200% 缩放可用 | ✅ ⧗（2026-08-08 按新形态重验） | **⧗「三标签」已不存在**。重验判据改为：等效 640×360 零横向溢出、内容重排为单列、滚到底后底栏 composer 与主操作完整可见（提交栏移到底部后它在首屏之下，靠纵向滚动到达——纵向滚动不违反 reflow 要求）|
| AC-10 六类流程端到端 | ✅ | 六类全覆盖（契约层脚本化模型，不烧 token）：正常完成、等待审批、历史查看、**审批拒绝**（deny+理由 → isError 含理由 → 正常收尾；卡片 status="denied" 保留理由与时间；概览保留只读审批记录）、**执行失败**（不崩 + stopReason="error" + finishedAt 非空；pending 审批在 error 路径同样转 expired）、**核查未通过**（verdict.passed=false + issues 非空 + 流中出现 source="rework"；裁决卡用错误色语义） |

## 发布门槛（第 14 节）

- P0 全部关闭 ✅（状态冲突、历史审批可操作、窄屏不可用三项均为 0）
- 桌面端核心流程通过 ✅；移动端（390px）核心流程通过 ✅
- 自动化无障碍扫描 ✅ axe-core 常驻门禁（六画面 violations = 0，含 incomplete 白名单与双向自检）+ 浏览器真机键盘走查
- 审批重复提交率 0 ✅（服务端幂等 409）
- 监控基线 ⚠️ 未建立（属运营项，非本轮范围）

## 无障碍专项实测（AC-05/AC-06，浏览器真机走查）

走查方式：真实浏览器加载运行中的页面，枚举 Tab 序、实测 `:focus-visible` 计算样式、
检查 ARIA 结构与 aria-live 播报、用真实 keydown 验证标签组键盘模式。

**实测通过**：Tab 序完整（10 个交互元素全可达、无焦点陷阱）；焦点可见性
（`solid 2px #0969da`）；`<label for="task-input">` 关联生效；aria-live 播报
"新任务已提交：…"。

**实测发现并修复的两处 ARIA 结构缺陷**（静态断言无法发现，只有真实 DOM 才暴露）：

1. **孤儿 `role="option"`**：运行项带 `role=option` + `aria-selected`，但父容器
   `#run-list` 没有 `role="listbox"`。ARIA 规范要求 option 必须在 listbox 内，
   否则屏幕阅读器无法播报"第 N 项 / 共 M 项"。→ 已补 `role="listbox"` + `aria-label`。
2. **tab 三件套不全**：有 `role=tablist`/`role=tab`，但没有 `role="tabpanel"`，
   tab 也没有 `aria-controls`——辅助技术不知道标签控制哪个面板。→ 已补
   `aria-controls` + tab 稳定 id + 面板 `role=tabpanel`/`aria-labelledby`。
   同时改为 APG roving tabindex（未选中项 `tabindex=-1`）并**配套实现
   ←/→/↑/↓/Home/End 方向键切换 + 切换后焦点跟随**——只加 roving 不加方向键
   会让未选中标签彻底键盘不可达，比改之前更糟。

三条对应回归测试已入库（32a/32b/32c），其中 32c 专门锁死"roving tabindex
必须配方向键"这一对约束，防止后续改动只做一半。

## axe-core 常驻门禁（s3e，委托方决定引入）

`test/ui-a11y.test.ts`（`@vitest-environment jsdom`，devDep 仅此一处使用；
`ui/` 运行时依赖保持为零）。六个画面——空态/运行列表/概览/运行日志/核查/窄屏
——`violations` 恒为空，每次 `npm test` 都跑。

**上线首跑即抓到两处真缺陷**（此前手写静态断言全绿）：

1. **空壳 listbox（critical）**：s3d 手工修复把 `role="listbox"` 静态写死进
   `index.html`，列表为空/加载中时无 `option` 子项，违反 `aria-required-children`。
   → 改为「身份与内容同生共死」：`renderRunList` 有项才挂 role/aria-label，空态摘掉。
2. **缺页面级 `h1`**：补 `<header class="sr-only"><h1>Agent Harness 控制台</h1></header>`。
   必须包在 `header`（banner 地标）内——裸 `h1` 游离于所有地标之外会触发 `region`
   规则，这是补 h1 时当场撞到的连锁反应。

**incomplete 白名单机制**：只断言 `violations` 为空并不够——axe 把「本环境判定不了」
的规则放进 `incomplete` 桶，既非通过也非失败，不盯就会静默溜过。已锁定已知三条
（`color-contrast` / `landmark-one-main` / `page-has-heading-one`，各注明原因），
新增任何一条即测试失败，强制有人看一眼。

**双向自检三条**（项目 checker 纪律）：植入 orphan option → `aria-required-parent`；
植入空壳 listbox → `aria-required-children`；植入悬空 `aria-labelledby` → 落待复核桶。
证明扫描器抓得住，而非「全绿只是没在看」。

**边界**：对比度、焦点可见性、目标尺寸等视觉类规则 jsdom 判不了，继续由
`ui-app.test.ts` 中从 `styles.css` 解析色对、按 WCAG 相对亮度公式实算的测试守护——
两者互补，不可互相替代。`npm audit` 新增 0 条（既有 7 条来自 `@anthropic-ai/sdk`
与 `@modelcontextprotocol/sdk`，与本次无关）。

## 剩余项建议

1. **NVDA 听感**（AC-06 的人工部分）：结构语义已由 axe 常驻门禁 + 浏览器走查覆盖，
   但真实屏幕阅读器的朗读顺序与措辞体验仍建议人工过一遍。
2. 监控基线（状态冲突率、审批处理耗时、结果定位耗时）属上线后运营项，非本轮范围。

## 附带修复的 harness 缺陷

s3c 期间从 verifier 日志中定位到一处 harness 自身缺陷并修复（不属报告范围，
但影响本轮核查质量）：`isReadOnlyCommand` 的只读命令白名单按 `|` 切段时不感知
引号，导致 `grep -n "a\|b" f` 这类带交替的合法只读命令被误判为管道链而拒绝；
verifier 因此反复退化重试、烧光核查轮次（s3b/s3c 连续两轮 fail-closed 空转的
元凶之一）。已改为引号感知扫描：引号内的 `|`/转义 `\|` 视作字面量，引号外的
重定向/链式/命令替换照旧拦截，未闭合引号按可疑拒绝。

单测总数：220（s3 五轮 +58，含白名单修复 1、无障碍结构 3、axe 门禁 12）。**⧗ 这是 v1 收尾时的数字，早已过期**（2026-08-08 实测 641 / 21 文件）——留着是因为它记的是"v1 那一轮加了多少"，不是"现在有多少"。要看当前规模跑 `npx vitest run`，别抄进文档。
