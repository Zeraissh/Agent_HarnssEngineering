/** Ambient types so tests can import the zero-build browser module. */

export function initThemeSelect(select: Element | null): unknown;
export function upgradeSelects(root: ParentNode): unknown[];
